// Тесты для Швеллера гнутого (обычного и оцинкованного)

const { calculateMetal } = require('./src/calculator');
const metalDatabase = require('./docs/database/metals.json');

console.log('═══════════════════════════════════════════════════════════════');
console.log('🧪 ТЕСТЫ ДЛЯ ШВЕЛЛЕРА ГНУТОГО');
console.log('═══════════════════════════════════════════════════════════════\n');

// Функция для тестирования
function testShvellerGnyt(testName, metalType, size, length, expected) {
  console.log(`📋 ${testName}`);
  console.log('─────────────────────────────────────────────────────────────\n');

  const params = {
    metalType,
    size,
    length
  };

  const result = calculateMetal(params, metalDatabase);

  // Отладка
  if (!result.success) {
    console.log('❌ ОШИБКА:', result.error);
    console.log('   Параметры:', params);
    return false;
  }

  const metal = metalDatabase.metals[metalType];
  const coefficient = metal.weights[size];
  const isGalvanized = metalType === 'shveller_gnyt_ocink';

  console.log('Параметры:');
  console.log(`  Тип: ${metal.name}`);
  console.log(`  Размер: ${size}`);
  console.log(`  Коэффициент: ${coefficient} (для ст3)`);
  console.log(`  Длина: ${length} м`);
  if (isGalvanized) {
    console.log(`  Оцинковка: +3% (×1.03)`);
  }
  console.log();

  console.log('Результат:');
  console.log(`  weightPerMeter (вес 1 м): ${result.weightPerMeter} кг`);
  console.log(`  weight (вес): ${result.weight} т\n`);

  console.log('Расчёт:');
  if (isGalvanized) {
    console.log(`  Формула: вес (т) = (коэффициент × длина × 7.85 × 1.03) / 1000`);
    console.log(`  вес = (${coefficient} × ${length} × 7.85 × 1.03) / 1000 = ${expected.weight} т`);
  } else {
    console.log(`  Формула: вес (т) = (коэффициент × длина × 7.85) / 1000`);
    console.log(`  вес = (${coefficient} × ${length} × 7.85) / 1000 = ${expected.weight} т`);
  }
  console.log(`  ⚠️ ВАЖНО: С × 7.85! Это гнутый швеллер!\n`);

  const diff = Math.abs(result.weight - expected.weight);

  console.log(`Ожидается: ~${expected.weight} т`);
  console.log(`Получено: ${result.weight} т`);
  console.log(`📊 Статус: ${diff < 0.01 ? '✅ PASSED' : '❌ FAILED'}\n`);

  return diff < 0.01;
}

// ============================================================================
// ТЕСТ 1: Малый размер (20х15х1) - ОБЫЧНЫЙ
// ============================================================================
// Коэффициент: 0.048 (из файла)
// Расчёт: (0.048 × 1000 × 7.85) / 1000 = 0.377 т

const test1 = testShvellerGnyt(
  'ТЕСТ 1: Размер 20х15х1, длина 1000 м (обычный)',
  'shveller_gnyt',
  '20х15х1',
  1000,
  {
    weight: 0.377  // 0.3768 округляется до 0.377
  }
);

// ============================================================================
// ТЕСТ 2: Малый размер (20х15х1) - ОЦИНКОВАННЫЙ
// ============================================================================
// Коэффициент: 0.048 (из файла)
// Расчёт: (0.048 × 1000 × 7.85 × 1.03) / 1000 = 0.388 т

const test2 = testShvellerGnyt(
  'ТЕСТ 2: Размер 20х15х1, длина 1000 м (оцинкованный)',
  'shveller_gnyt_ocink',
  '20х15х1',
  1000,
  {
    weight: 0.388  // 0.388104 округляется до 0.388
  }
);

// ============================================================================
// ТЕСТ 3: Малый размер (20х40х2) - ОБЫЧНЫЙ
// ============================================================================
// Коэффициент: 0.192 (из файла)
// Расчёт: (0.192 × 500 × 7.85) / 1000 = 0.754 т

const test3 = testShvellerGnyt(
  'ТЕСТ 3: Размер 20х40х2, длина 500 м (обычный)',
  'shveller_gnyt',
  '20х40х2',
  500,
  {
    weight: 0.754  // 0.7536 округляется до 0.754
  }
);

// ============================================================================
// ТЕСТ 4: Малый размер (20х40х2) - ОЦИНКОВАННЫЙ
// ============================================================================
// Коэффициент: 0.192 (из файла)
// Расчёт: (0.192 × 500 × 7.85 × 1.03) / 1000 = 0.777 т

const test4 = testShvellerGnyt(
  'ТЕСТ 4: Размер 20х40х2, длина 500 м (оцинкованный)',
  'shveller_gnyt_ocink',
  '20х40х2',
  500,
  {
    weight: 0.777  // 0.776208 округляется до 0.777
  }
);

// ============================================================================
// ТЕСТ 5: Средний размер (60х40х2.5) - ОБЫЧНЫЙ
// ============================================================================
// Коэффициент: 0.3375 (из файла)
// Расчёт: (0.3375 × 300 × 7.85) / 1000 = 0.795 т

const test5 = testShvellerGnyt(
  'ТЕСТ 5: Размер 60х40х2.5, длина 300 м (обычный)',
  'shveller_gnyt',
  '60х40х2.5',
  300,
  {
    weight: 0.795  // 0.79509375 округляется до 0.795
  }
);

// ============================================================================
// ТЕСТ 6: Средний размер (60х40х2.5) - ОЦИНКОВАННЫЙ
// ============================================================================
// Коэффициент: 0.3375 (из файла)
// Расчёт: (0.3375 × 300 × 7.85 × 1.03) / 1000 = 0.819 т

const test6 = testShvellerGnyt(
  'ТЕСТ 6: Размер 60х40х2.5, длина 300 м (оцинкованный)',
  'shveller_gnyt_ocink',
  '60х40х2.5',
  300,
  {
    weight: 0.819  // 0.818946563 округляется до 0.819
  }
);

// ============================================================================
// ТЕСТ 7: Большой размер (200х100х8) - ОБЫЧНЫЙ
// ============================================================================
// Коэффициент: 3.072 (из файла)
// Расчёт: (3.072 × 100 × 7.85) / 1000 = 2.412 т

const test7 = testShvellerGnyt(
  'ТЕСТ 7: Размер 200х100х8, длина 100 м (обычный)',
  'shveller_gnyt',
  '200х100х8',
  100,
  {
    weight: 2.412  // 2.41152 округляется до 2.412
  }
);

// ============================================================================
// ТЕСТ 8: Большой размер (200х100х8) - ОЦИНКОВАННЫЙ
// ============================================================================
// Коэффициент: 3.072 (из файла)
// Расчёт: (3.072 × 100 × 7.85 × 1.03) / 1000 = 2.484 т

const test8 = testShvellerGnyt(
  'ТЕСТ 8: Размер 200х100х8, длина 100 м (оцинкованный)',
  'shveller_gnyt_ocink',
  '200х100х8',
  100,
  {
    weight: 2.484  // 2.48386560 округляется до 2.484
  }
);

// ============================================================================
// ТЕСТ 9: Максимальный размер (300х150х10) - ОБЫЧНЫЙ
// ============================================================================
// Коэффициент: 5.8 (из файла)
// Расчёт: (5.8 × 50 × 7.85) / 1000 = 2.277 т

const test9 = testShvellerGnyt(
  'ТЕСТ 9: Размер 300х150х10, длина 50 м (обычный)',
  'shveller_gnyt',
  '300х150х10',
  50,
  {
    weight: 2.277  // 2.2765 округляется до 2.277
  }
);

// ============================================================================
// ТЕСТ 10: Максимальный размер (300х150х10) - ОЦИНКОВАННЫЙ
// ============================================================================
// Коэффициент: 5.8 (из файла)
// Расчёт: (5.8 × 50 × 7.85 × 1.03) / 1000 = 2.345 т

const test10 = testShvellerGnyt(
  'ТЕСТ 10: Размер 300х150х10, длина 50 м (оцинкованный)',
  'shveller_gnyt_ocink',
  '300х150х10',
  50,
  {
    weight: 2.345  // 2.3447795 округляется до 2.345
  }
);

// ============================================================================
// ИТОГИ
// ============================================================================

console.log('═══════════════════════════════════════════════════════════════');
console.log('📊 ИТОГИ ТЕСТИРОВАНИЯ');
console.log('═══════════════════════════════════════════════════════════════\n');

const allPassed = test1 && test2 && test3 && test4 && test5 && test6 && test7 && test8 && test9 && test10;

if (allPassed) {
  console.log('✅ ВСЕ ТЕСТЫ ПРОЙДЕНЫ УСПЕШНО!\n');
  console.log('🎉 Швеллер гнутый работает правильно:');
  console.log('   1. Формула работает: (коэф × длина × 7.85) / 1000');
  console.log('   2. Формула для оцинкованного работает: (коэф × длина × 7.85 × 1.03) / 1000');
  console.log('   3. ⚠️ ВАЖНО: С × 7.85! Это гнутый швеллер!');
  console.log('   4. Оцинковка добавляет +3% к весу');
  console.log('   5. Все размеры работают корректно (от малых до максимальных)');
  console.log('\n📊 Статистика:');
  console.log('   Размеров в базе (обычный): 2428');
  console.log('   Размеров в базе (оцинкованный): 2428');
  console.log('   ВСЕГО размеров: 4856');
  console.log('   Формула: shveller_gnyt_linear / shveller_gnyt_galv_linear');
  console.log('   ГОСТ: ГОСТы и ТУ на швеллер гнутый');
} else {
  console.log('❌ НЕКОТОРЫЕ ТЕСТЫ НЕ ПРОШЛИ!\n');
  console.log(`Тест 1 (20х15х1, обычный): ${test1 ? '✅' : '❌'}`);
  console.log(`Тест 2 (20х15х1, оцинкованный): ${test2 ? '✅' : '❌'}`);
  console.log(`Тест 3 (20х40х2, обычный): ${test3 ? '✅' : '❌'}`);
  console.log(`Тест 4 (20х40х2, оцинкованный): ${test4 ? '✅' : '❌'}`);
  console.log(`Тест 5 (60х40х2.5, обычный): ${test5 ? '✅' : '❌'}`);
  console.log(`Тест 6 (60х40х2.5, оцинкованный): ${test6 ? '✅' : '❌'}`);
  console.log(`Тест 7 (200х100х8, обычный): ${test7 ? '✅' : '❌'}`);
  console.log(`Тест 8 (200х100х8, оцинкованный): ${test8 ? '✅' : '❌'}`);
  console.log(`Тест 9 (300х150х10, обычный): ${test9 ? '✅' : '❌'}`);
  console.log(`Тест 10 (300х150х10, оцинкованный): ${test10 ? '✅' : '❌'}`);
}

console.log('\n═══════════════════════════════════════════════════════════════');
