// Тесты для Швеллера (обычного и оцинкованного)

const { calculateMetal } = require('./src/calculator');
const metalDatabase = require('./docs/database/metals.json');

console.log('═══════════════════════════════════════════════════════════════');
console.log('🧪 ТЕСТЫ ДЛЯ ШВЕЛЛЕРА');
console.log('═══════════════════════════════════════════════════════════════\n');

// Функция для тестирования
function testShveller(testName, metalType, size, length, expected) {
  console.log(`📋 ${testName}`);
  console.log('─────────────────────────────────────────────────────────────\n');

  const params = {
    metalType,
    size,
    length
  };

  const result = calculateMetal(params, metalDatabase);

  // Отладка
  if (!result.success) {
    console.log('❌ ОШИБКА:', result.error);
    console.log('   Параметры:', params);
    return false;
  }

  const metal = metalDatabase.metals[metalType];
  const coefficient = metal.weights[size];
  const isGalvanized = metalType === 'shveller_ocink';

  console.log('Параметры:');
  console.log(`  Тип: ${metal.name}`);
  console.log(`  Размер: ${size}`);
  console.log(`  Коэффициент: ${coefficient} кг/м`);
  console.log(`  Длина: ${length} м`);
  if (isGalvanized) {
    console.log(`  Оцинковка: +3% (×1.03)`);
  }
  console.log();

  console.log('Результат:');
  console.log(`  weightPerMeter (вес 1 м): ${result.weightPerMeter} кг`);
  console.log(`  weight (вес): ${result.weight} т\n`);

  console.log('Расчёт:');
  if (isGalvanized) {
    console.log(`  Формула: вес (т) = (коэффициент × длина × 1.03) / 1000`);
    console.log(`  вес = (${coefficient} × ${length} × 1.03) / 1000 = ${expected.weight} т`);
  } else {
    console.log(`  Формула: вес (т) = (коэффициент × длина) / 1000`);
    console.log(`  вес = (${coefficient} × ${length}) / 1000 = ${expected.weight} т`);
  }
  console.log(`  ⚠️ БЕЗ × 7.85! Коэффициенты уже в кг/м`);
  console.log();

  const diff = Math.abs(result.weight - expected.weight);

  console.log(`Ожидается: ~${expected.weight} т`);
  console.log(`Получено: ${result.weight} т`);
  console.log(`📊 Статус: ${diff < 0.01 ? '✅ PASSED' : '❌ FAILED'}\n`);

  return diff < 0.01;
}

// ============================================================================
// ТЕСТ 1: Малый размер (5П) - обычный
// ============================================================================
// Коэффициент: 4.84 кг/м
// Расчёт: (4.84 × 1000) / 1000 = 4.84 т

const test1 = testShveller(
  'ТЕСТ 1: Размер 5П, длина 1000 м (обычный)',
  'shveller',
  '5П',
  1000,
  {
    weight: 4.84
  }
);

// ============================================================================
// ТЕСТ 2: Малый размер (5П) - оцинкованный
// ============================================================================
// Коэффициент: 4.84 кг/м
// Расчёт: (4.84 × 1000 × 1.03) / 1000 = 4.9852 т

const test2 = testShveller(
  'ТЕСТ 2: Размер 5П, длина 1000 м (оцинкованный)',
  'shveller_ocink',
  '5П',
  1000,
  {
    weight: 4.99  // 4.9852 округляется до 4.99
  }
);

// ============================================================================
// ТЕСТ 3: Средний размер (16С) - обычный
// ============================================================================
// Коэффициент: 17.53 кг/м
// Расчёт: (17.53 × 1000) / 1000 = 17.53 т

const test3 = testShveller(
  'ТЕСТ 3: Размер 16С, длина 1000 м (обычный)',
  'shveller',
  '16С',
  1000,
  {
    weight: 17.53
  }
);

// ============================================================================
// ТЕСТ 4: Средний размер (16С) - оцинкованный
// ============================================================================
// Коэффициент: 17.53 кг/м
// Расчёт: (17.53 × 1000 × 1.03) / 1000 = 18.0559 т

const test4 = testShveller(
  'ТЕСТ 4: Размер 16С, длина 1000 м (оцинкованный)',
  'shveller_ocink',
  '16С',
  1000,
  {
    weight: 18.06  // 18.0559 округляется до 18.06
  }
);

// ============================================================================
// ТЕСТ 5: Большой размер (30П) - обычный
// ============================================================================
// Коэффициент: 31.8 кг/м
// Расчёт: (31.8 × 500) / 1000 = 15.9 т

const test5 = testShveller(
  'ТЕСТ 5: Размер 30П, длина 500 м (обычный)',
  'shveller',
  '30П',
  500,
  {
    weight: 15.90
  }
);

// ============================================================================
// ТЕСТ 6: Большой размер (30П) - оцинкованный
// ============================================================================
// Коэффициент: 31.8 кг/м
// Расчёт: (31.8 × 500 × 1.03) / 1000 = 16.377 т

const test6 = testShveller(
  'ТЕСТ 6: Размер 30П, длина 500 м (оцинкованный)',
  'shveller_ocink',
  '30П',
  500,
  {
    weight: 16.38  // 16.377 округляется до 16.38
  }
);

// ============================================================================
// ТЕСТ 7: Максимальный размер (UPN400) - обычный
// ============================================================================
// Коэффициент: 71.8 кг/м
// Расчёт: (71.8 × 100) / 1000 = 7.18 т

const test7 = testShveller(
  'ТЕСТ 7: Размер UPN400, длина 100 м (обычный)',
  'shveller',
  'UPN400',
  100,
  {
    weight: 7.18
  }
);

// ============================================================================
// ТЕСТ 8: Максимальный размер (UPN400) - оцинкованный
// ============================================================================
// Коэффициент: 71.8 кг/м
// Расчёт: (71.8 × 100 × 1.03) / 1000 = 7.3954 т

const test8 = testShveller(
  'ТЕСТ 8: Размер UPN400, длина 100 м (оцинкованный)',
  'shveller_ocink',
  'UPN400',
  100,
  {
    weight: 7.40  // 7.3954 округляется до 7.40
  }
);

// ============================================================================
// ТЕСТ 9: Дробная длина (24У) - обычный
// ============================================================================
// Коэффициент: 24 кг/м
// Расчёт: (24 × 37.5) / 1000 = 0.9 т

const test9 = testShveller(
  'ТЕСТ 9: Размер 24У, длина 37.5 м (обычный)',
  'shveller',
  '24У',
  37.5,
  {
    weight: 0.90
  }
);

// ============================================================================
// ТЕСТ 10: Дробная длина (24У) - оцинкованный
// ============================================================================
// Коэффициент: 24 кг/м
// Расчёт: (24 × 37.5 × 1.03) / 1000 = 0.927 т

const test10 = testShveller(
  'ТЕСТ 10: Размер 24У, длина 37.5 м (оцинкованный)',
  'shveller_ocink',
  '24У',
  37.5,
  {
    weight: 0.93  // 0.927 округляется до 0.93
  }
);

// ============================================================================
// ИТОГИ
// ============================================================================

console.log('═══════════════════════════════════════════════════════════════');
console.log('📊 ИТОГИ ТЕСТИРОВАНИЯ');
console.log('═══════════════════════════════════════════════════════════════\n');

const allPassed = test1 && test2 && test3 && test4 && test5 && test6 && test7 && test8 && test9 && test10;

if (allPassed) {
  console.log('✅ ВСЕ ТЕСТЫ ПРОЙДЕНЫ УСПЕШНО!\n');
  console.log('🎉 Швеллер работает правильно:');
  console.log('   1. Формула работает: вес = (коэффициент × длина) / 1000');
  console.log('   2. Формула для оцинкованного работает: вес = (коэффициент × длина × 1.03) / 1000');
  console.log('   3. Оцинковка добавляет +3% к весу');
  console.log('   4. Все размеры работают корректно (от малых до больших)');
  console.log('   5. БЕЗ × 7.85! Коэффициенты уже в кг/м');
  console.log('\n📊 Статистика:');
  console.log('   Размеров в базе (обычный): 112');
  console.log('   Размеров в базе (оцинкованный): 112');
  console.log('   ВСЕГО размеров: 224');
  console.log('   Формула: shveller_simple / shveller_galv_simple');
  console.log('   ГОСТ: ГОСТы и ТУ на швеллер');
} else {
  console.log('❌ НЕКОТОРЫЕ ТЕСТЫ НЕ ПРОШЛИ!\n');
  console.log(`Тест 1 (5П, обычный): ${test1 ? '✅' : '❌'}`);
  console.log(`Тест 2 (5П, оцинкованный): ${test2 ? '✅' : '❌'}`);
  console.log(`Тест 3 (16С, обычный): ${test3 ? '✅' : '❌'}`);
  console.log(`Тест 4 (16С, оцинкованный): ${test4 ? '✅' : '❌'}`);
  console.log(`Тест 5 (30П, обычный): ${test5 ? '✅' : '❌'}`);
  console.log(`Тест 6 (30П, оцинкованный): ${test6 ? '✅' : '❌'}`);
  console.log(`Тест 7 (UPN400, обычный): ${test7 ? '✅' : '❌'}`);
  console.log(`Тест 8 (UPN400, оцинкованный): ${test8 ? '✅' : '❌'}`);
  console.log(`Тест 9 (24У, обычный): ${test9 ? '✅' : '❌'}`);
  console.log(`Тест 10 (24У, оцинкованный): ${test10 ? '✅' : '❌'}`);
}

console.log('\n═══════════════════════════════════════════════════════════════');
