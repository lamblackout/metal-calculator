# 📋 ИНСТРУКЦИЯ ДЛЯ ТИМЛИДА
## Внедрение данных о весах и ГОСТах металлопроката и метизов

---

## 🎯 ЦЕЛЬ
Добавить в калькулятор проекта данные о **весах**, **коэффициентах**, **ГОСТах** и **марках стали** для 10 категорий товаров, собранные с сайта 23met.ru.

---

## 📦 ЧТО ПОЛУЧЕНО

### **11 файлов с данными:**

#### **1. Металлопрокат (с марками стали):**
- **Квадрат.txt** — содержит:
  - ✅ Размеры квадрата (мм)
  - ✅ Коэффициенты для расчёта веса
  - ✅ Марки стали (137 марок)
  - ✅ Коэффициенты плотности для каждой марки стали
  - ⚠️ **ГОСТ вынесен в отдельный файл** (см. ниже)

- **Квадраты_ГОСТ.txt** — содержит:
  - ✅ Полный список ГОСТов и ТУ для квадратов
  - ✅ Описания каждого ГОСТа

#### **2. Метизы (веса и ГОСТы):**
- **Болты.txt** — веса (кг/шт) + ГОСТ для каждого размера
- **Винты.txt** — веса (кг/шт) + ГОСТ для каждого размера
- **Гайки.txt** — веса (кг/шт) + ГОСТ для каждого размера
- **Гвозди.txt** — веса (кг/шт) + ГОСТ для каждого размера
- **Саморезы.txt** — веса (кг/шт) + ГОСТ для каждого размера
- **Шайбы.txt** — веса (кг/шт) + ГОСТ для каждого размера
- **Шпильки.txt** — веса (кг/шт) + ГОСТ для каждого размера
- **Шплинты.txt** — веса (кг/шт) + ГОСТ для каждого размера
- **Шурупы.txt** — веса (кг/шт) + ГОСТ для каждого размера

---

## 📊 СТРУКТУРА ДАННЫХ

### **Для Квадрата (металлопрокат):**

```json
{
  "sizes": ["4", "5", "6", "7", ...],          // Массив размеров (мм)
  
  "weights": {                                   // Коэффициенты веса
    "4": 0.016,
    "5": 0.025,
    ...
  },
  
  "steelTypes": ["ст3", "09Г2С", ...],         // Массив марок стали
  
  "steelCoefficients": {                        // Плотность каждой марки
    "ст3": 7.85,
    "09Г2С": 7.85,
    ...
  },
  
  "gosts": {                                     // ГОСТ (общий для всех)
    "all": "ГОСТы и ТУ"
  }
}
```

**Формула расчёта веса для квадрата:**
```
Вес (кг/м) = weights[размер] × steelCoefficients[марка_стали]
```

**Пример:**
- Квадрат 10 мм, сталь ст3
- Вес = 0.1 × 7.85 = **0.785 кг/м**

---

### **Для Метизов (Болты, Винты, Гайки и др.):**

```json
{
  "sizes": ["1.6х2", "1.6х3", ...],            // Массив размеров
  
  "weights": {                                   // Вес ОДНОЙ штуки (кг)
    "1.6х2": 0.104,
    "1.6х3": 0.115,
    ...
  },
  
  "gosts": {                                     // ГОСТ для каждого размера
    "1.6х2": "ГОСТ 7805-70",
    "1.6х3": "ГОСТ 7805-70",
    ...
  }
}
```

**Особенность метизов:**
- Вес указан **за 1 штуку** (в килограммах)
- Для расчёта общего веса: `Вес = weights[размер] × количество_штук`
- ГОСТ привязан к **каждому размеру отдельно**

---

## 🔧 ЧТО НУЖНО СДЕЛАТЬ

### **Этап 1: Распарсить данные из файлов**

Все файлы содержат **логи из консоли браузера** после выполнения скриптов парсинга. Нужные данные находятся в JSON-блоках внутри логов.

#### **Пример структуры файла (Болты.txt):**
```
VM51:190 ✅ СБОР ДАННЫХ ЗАВЕРШЁН
...
VM51:199 // ===== 1. РАЗМЕРЫ БОЛТОВ =====
VM51:200 ["1.6х2", "1.6х3", ...]
VM51:201 
VM51:202 // ===== 2. ВЕСА БОЛТОВ (кг за штуку) =====
VM51:203 {
  "1.6х2": 0.104,
  "1.6х3": 0.115,
  ...
}
VM51:204 
VM51:205 // ===== 3. ГОСТы БОЛТОВ (по размерам) =====
VM51:206 {
  "1.6х2": "ГОСТ 7805-70",
  ...
}
```

#### **Как извлечь JSON:**
1. Открой каждый `.txt` файл
2. Найди секции с комментариями `// ===== 1. ...`, `// ===== 2. ...` и т.д.
3. Скопируй JSON-данные (массивы и объекты) после каждого комментария
4. Эти JSON можно использовать напрямую в коде или БД

---

### **Этап 2: Внедрить данные в калькулятор**

#### **Вариант А: Хранение в базе данных**

**Создать таблицы:**

##### **1. Таблица для металлопроката (Квадрат):**
```sql
CREATE TABLE kvadrat_sizes (
    id SERIAL PRIMARY KEY,
    size VARCHAR(10) NOT NULL,           -- "4", "5", "6"...
    weight_coefficient DECIMAL(12,6)     -- 0.016, 0.025...
);

CREATE TABLE steel_types (
    id SERIAL PRIMARY KEY,
    name VARCHAR(50) NOT NULL,           -- "ст3", "09Г2С"...
    density DECIMAL(12,6)                -- 7.85, 7.85...
);

CREATE TABLE kvadrat_gosts (
    id SERIAL PRIMARY KEY,
    gost_name VARCHAR(100),
    description TEXT
);
```

##### **2. Таблица для метизов:**
```sql
CREATE TABLE fasteners (
    id SERIAL PRIMARY KEY,
    category VARCHAR(50) NOT NULL,       -- "bolty", "vinty", "gaiki"...
    size VARCHAR(20) NOT NULL,           -- "1.6х2", "М8"...
    weight DECIMAL(12,6),                -- 0.104 (кг/шт)
    gost VARCHAR(100)                    -- "ГОСТ 7805-70"
);

-- Индекс для быстрого поиска
CREATE INDEX idx_fasteners_category_size 
ON fasteners(category, size);
```

**Заполнить таблицы:**
- Используй JSON из файлов для импорта
- Можно написать скрипт миграции (Python/Node.js)

---

#### **Вариант Б: Хранение в коде (статические файлы)**

**Создать JSON-конфиги:**

```
/config
  /weights
    - kvadrat.json
    - bolty.json
    - vinty.json
    - gaiki.json
    - gvozdi.json
    - samorezy.json
    - shaiby.json
    - shpilki.json
    - shplynty.json
    - shyrypy.json
  /gosts
    - kvadrat_gosts.json
    - fasteners_gosts.json
```

**Пример kvadrat.json:**
```json
{
  "sizes": ["4", "5", "6", ...],
  "weights": {
    "4": 0.016,
    "5": 0.025,
    ...
  },
  "steelTypes": ["ст3", "09Г2С", ...],
  "steelCoefficients": {
    "ст3": 7.85,
    "09Г2С": 7.85,
    ...
  }
}
```

**Пример bolty.json:**
```json
{
  "sizes": ["1.6х2", "1.6х3", ...],
  "weights": {
    "1.6х2": 0.104,
    "1.6х3": 0.115,
    ...
  },
  "gosts": {
    "1.6х2": "ГОСТ 7805-70",
    "1.6х3": "ГОСТ 7805-70",
    ...
  }
}
```

---

### **Этап 3: Реализовать логику расчёта**

#### **Для Квадрата (металлопрокат):**

**Псевдокод:**
```javascript
function calculateKvadratWeight(size, steelType, length) {
  // Получаем коэффициент размера
  const sizeCoef = kvadratData.weights[size];
  
  // Получаем плотность стали
  const steelDensity = kvadratData.steelCoefficients[steelType];
  
  // Формула: Вес (кг) = коэффициент × плотность × длина (м)
  const weightPerMeter = sizeCoef * steelDensity;
  const totalWeight = weightPerMeter * length;
  
  return {
    weightPerMeter: weightPerMeter.toFixed(3),
    totalWeight: totalWeight.toFixed(3),
    gost: "ГОСТы и ТУ (см. полный список)"
  };
}

// Пример использования:
calculateKvadratWeight("10", "ст3", 6); 
// → { weightPerMeter: "0.785", totalWeight: "4.710", gost: "..." }
```

---

#### **Для Метизов (Болты, Винты и др.):**

**Псевдокод:**
```javascript
function calculateFastenerWeight(category, size, quantity) {
  // Получаем вес одной штуки
  const weightPerPiece = fastenersData[category].weights[size];
  
  // Получаем ГОСТ для размера
  const gost = fastenersData[category].gosts[size];
  
  // Формула: Вес (кг) = вес_штуки × количество
  const totalWeight = weightPerPiece * quantity;
  
  return {
    weightPerPiece: weightPerPiece.toFixed(6),
    totalWeight: totalWeight.toFixed(3),
    gost: gost
  };
}

// Пример использования:
calculateFastenerWeight("bolty", "1.6х2", 1000);
// → { weightPerPiece: "0.000104", totalWeight: "0.104", gost: "ГОСТ 7805-70" }
```

---

### **Этап 4: Интеграция с UI**

#### **Для калькулятора квадрата:**

1. **Dropdown с размерами** — заполнить из `sizes`
2. **Dropdown с марками стали** — заполнить из `steelTypes`
3. **Поле "Длина (м)"** — ввод пользователя
4. **Кнопка "Рассчитать"** — вызов функции расчёта
5. **Вывод результата:**
   - Вес за 1 метр (кг/м)
   - Общий вес (кг)
   - ГОСТ + ссылка на полный список (из файла `Квадраты_ГОСТ.txt`)

#### **Для калькулятора метизов:**

1. **Dropdown с категорией** — Болты / Винты / Гайки и т.д.
2. **Dropdown с размерами** — заполнить из `sizes` выбранной категории
3. **Поле "Количество (шт)"** — ввод пользователя
4. **Кнопка "Рассчитать"** — вызов функции расчёта
5. **Вывод результата:**
   - Вес 1 шт (кг)
   - Общий вес (кг)
   - ГОСТ для выбранного размера

---

## ⚠️ ВАЖНЫЕ МОМЕНТЫ

### **1. Про ГОСТ для Квадрата:**
- В файле **Квадрат.txt** указан только общий ГОСТ: `"ГОСТы и ТУ"`
- **Полный список ГОСТов** находится в файле **Квадраты_ГОСТ.txt**
- Рекомендация: добавить ссылку типа "Посмотреть все ГОСТы" → открывает модальное окно со списком

### **2. Особенность размера "────" в Квадрате:**
- В данных есть размер `"────"` с коэффициентом `0.016`
- Это **разделитель** в оригинальном селекте на сайте
- **Исключить** этот размер при импорте или отметить как `disabled`

### **3. Точность весов:**
- **Для квадрата:** коэффициенты округлены до 12 знаков после запятой
- **Для метизов:** веса округлены до 6 знаков после запятой (некоторые до 12)
- При расчётах использовать эти значения напрямую, результат округлять до 3 знаков

### **4. Названия категорий метизов (внутренние):**
```
bolty     → Болты
vinty     → Винты
gaiki     → Гайки
gvozdi    → Гвозди
samorezy  → Саморезы
shaiby    → Шайбы
shpilki   → Шпильки
shplynty  → Шплинты
shyrypy   → Шурупы
```

---

## 📝 ЧЕКЛИСТ ДЛЯ ВНЕДРЕНИЯ

- [ ] **Извлечь JSON из 11 файлов**
  - [ ] Квадрат.txt (размеры, коэффициенты, марки стали)
  - [ ] Квадраты_ГОСТ.txt (список ГОСТов)
  - [ ] 9 файлов метизов (веса + ГОСТы)

- [ ] **Создать структуру хранения**
  - [ ] Вариант А: таблицы в БД
  - [ ] Вариант Б: JSON-конфиги

- [ ] **Импортировать данные**
  - [ ] Исключить размер "────" из Квадрата
  - [ ] Проверить корректность всех JSON

- [ ] **Реализовать функции расчёта**
  - [ ] calculateKvadratWeight(size, steel, length)
  - [ ] calculateFastenerWeight(category, size, quantity)

- [ ] **Интегрировать с UI**
  - [ ] Dropdown с размерами (динамический)
  - [ ] Dropdown с марками стали (для квадрата)
  - [ ] Поля ввода (длина/количество)
  - [ ] Вывод результата (вес + ГОСТ)

- [ ] **Добавить страницу с ГОСТами для квадрата**
  - [ ] Модальное окно или отдельная страница
  - [ ] Содержимое из файла Квадраты_ГОСТ.txt

- [ ] **Тестирование**
  - [ ] Проверить расчёты вручную для нескольких примеров
  - [ ] Сравнить результаты с калькулятором на 23met.ru
  - [ ] Убедиться, что все размеры и ГОСТы отображаются корректно

---

## 🧪 ПРИМЕРЫ ДЛЯ ТЕСТИРОВАНИЯ

### **Квадрат:**

**Тест 1:**
- Размер: 10 мм
- Сталь: ст3
- Длина: 6 м
- **Ожидаемый результат:**
  - Вес за метр: 0.785 кг/м
  - Общий вес: 4.710 кг

**Тест 2:**
- Размер: 20 мм
- Сталь: 09Г2С
- Длина: 3 м
- **Ожидаемый результат:**
  - Вес за метр: 3.140 кг/м (0.4 × 7.85)
  - Общий вес: 9.420 кг

---

### **Болты:**

**Тест 1:**
- Размер: 6х20
- Количество: 100 шт
- **Ожидаемый результат:**
  - Вес 1 шт: (из файла Болты.txt)
  - Общий вес: вес_1шт × 100
  - ГОСТ: (из файла Болты.txt)

**Тест 2:**
- Размер: 10х50
- Количество: 50 шт
- **Ожидаемый результат:**
  - Вес 1 шт: (из файла Болты.txt)
  - Общий вес: вес_1шт × 50
  - ГОСТ: (из файла Болты.txt)

---

## 📞 КОНТАКТЫ

Если возникнут вопросы по структуре данных или логике расчётов:
- Все данные собраны с официального калькулятора 23met.ru
- Формулы расчёта взяты из исходного кода сайта
- Данные актуальны на ноябрь 2025

---

## ✅ ИТОГ

После внедрения калькулятор будет:
1. ✅ Показывать все доступные размеры для каждой категории
2. ✅ Рассчитывать точный вес на основе реальных коэффициентов
3. ✅ Отображать соответствующий ГОСТ для каждого размера
4. ✅ Для квадрата — учитывать выбранную марку стали
5. ✅ Для метизов — рассчитывать вес по количеству штук

**Все данные готовы к использованию!** 🚀
