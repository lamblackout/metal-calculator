# 🏗️ АРХИТЕКТУРА МЕТАЛЛОКАЛЬКУЛЯТОРА

**Версия:** 2.0
**Дата:** 2025-11-05
**Статус:** Production Ready ✅

---

## 📋 ОГЛАВЛЕНИЕ

1. [Обзор проекта](#обзор-проекта)
2. [Структура файлов](#структура-файлов)
3. [Архитектура кода](#архитектура-кода)
4. [База данных](#база-данных)
5. [Пользовательский интерфейс](#пользовательский-интерфейс)
6. [Система тестирования](#система-тестирования)
7. [Процесс сборки и деплоя](#процесс-сборки-и-деплоя)
8. [Ключевые концепции](#ключевые-концепции)

---

## 🎯 ОБЗОР ПРОЕКТА

### Назначение
Калькулятор металлопроката для автоматического расчёта веса, длины и количества штук металла с округлением кратно целым штукам.

### Ключевые особенности
- ✅ 76 типов металла, 5,700+ размеров
- ✅ Автоматическое округление кратно штукам (всегда вверх)
- ✅ Пересчёт всех параметров от округлённых штук
- ✅ Взаимозависимые поля (заполнил одно → остальные автоматом)
- ✅ Формулы по ГОСТам
- ✅ Готов к интеграции с n8n

### Технологии
- **Frontend:** HTML5, CSS3, Vanilla JavaScript
- **Backend:** Node.js (для тестов и сборки)
- **Bundle:** Custom build script
- **Деплой:** GitHub Pages
- **Тесты:** Custom test scripts

---

## 📁 СТРУКТУРА ФАЙЛОВ

```
metal-calculator/
├── 📂 src/                          # Исходный код
│   ├── calculator.js               # Основная логика расчётов
│   └── formulas.js                 # Математические формулы
│
├── 📂 database/                     # База данных
│   └── metals.json                 # 76 металлов, 5700+ размеров (457 KB)
│
├── 📂 dist/                         # Собранные bundle'ы
│   ├── calculator.bundle.js        # Node.js версия
│   └── calculator.browser.js       # Browser версия
│
├── 📂 docs/                         # GitHub Pages (production)
│   ├── calculator.html             # Главная страница калькулятора
│   ├── 📂 database/
│   │   └── metals.json             # Копия БД для production
│   └── 📂 dist/
│       └── calculator.browser.js   # Копия bundle для production
│
├── 📂 test-*.js                     # Тестовые скрипты
├── build.js                         # Скрипт сборки bundle
├── import-fasteners-full.js        # Импорт крепежа из .txt файлов
├── ARCHITECTURE.md                  # 📘 ЭТОТ ФАЙЛ
├── DEVELOPMENT_STATUS.md            # 📗 Текущий статус разработки
└── package.json                     # Зависимости проекта
```

---

## 🧠 АРХИТЕКТУРА КОДА

### 1. Основная логика (src/calculator.js)

**Главная функция:** `calculateMetal(params, metalDatabase)`

**Входные параметры:**
```javascript
{
  metalType: 'armature_a3',        // Тип металла
  size: 12,                        // Размер
  weight: 1,                       // Вес в тоннах (опционально)
  length: 1000,                    // Длина в метрах (опционально)
  pieces: 86,                      // Штуки (опционально)
  steelType: 'ст3'                // Марка стали (для катанки)
}
```

**Выходные данные:**
```javascript
{
  success: true,
  metalType: "Арматура А3",
  size: 12,

  // Запрошенные значения
  requested: {
    value: 1000,
    unit: "length",
    label: "1000 м"
  },

  // Фактические значения (кратно штукам)
  actual: {
    weight: 0.893,      // т
    length: 1006.2,     // м
    pieces: 86          // шт
  },

  // Разница
  difference: {
    length: "+6.2 м"
  },

  // Дополнительно
  weightPerMeter: 0.888,
  standardLength: 11.7,
  gost: "ГОСТ 5781-82"
}
```

### Ключевая логика округления

**Алгоритм:**

1. Пользователь вводит значение (например, 1000м)
2. Рассчитываем промежуточное количество штук: `1000 / 11.7 = 85.47`
3. Округляем ВВЕРХ: `Math.ceil(85.47) = 86` штук
4. Пересчитываем от штук:
   - Длина: `86 × 11.7 = 1006.2` м
   - Вес: `1006.2 × 0.888 = 893.5` кг

**Код (src/calculator.js:146-163):**
```javascript
} else if (params.length) {
  const requestedLength = params.length;
  const pieceLength = standardLength || 1;  // Fallback на 1 метр

  // Округляем штуки ВВЕРХ
  pieces = Math.ceil(requestedLength / pieceLength);

  // Пересчитываем от округлённых штук
  length = pieces * pieceLength;
  const actualWeightKg = weightPerMeter * length;
  weight = actualWeightKg / 1000;
}
```

### Обработка разных типов металла

**1. Стандартные металлы (арматура, балки, швеллеры)**
- Формула: `масса_метра × длина`
- Пример: Арматура 12мм = 0.888 кг/м

**2. Катанка**
- Формула: `коэф_размера × коэф_стали`
- Зависит от марки стали (ст3, ст5, 45, и т.д.)

**3. Крепёж (болты, гайки, шайбы)**
- Вес на 1000 штук в БД
- Формула: `(вес_на_1000 / 1000) × количество`

**4. Листы**
- Формула: `толщина(мм) × ширина(м) × длина(м) × 7.85`

**5. Канаты**
- Табличные значения (`useKilograms: true`)

---

### 2. Формулы (src/formulas.js)

**Константы:**
```javascript
const STEEL_DENSITY = 7850;  // кг/м³ (плотность стали)
```

**Основные функции:**

**`calculateLengthFromWeight(weightKg, weightPerMeter)`**
```javascript
// Длина = Вес / ВесЗаМетр
return weightKg / weightPerMeter;
```

**`calculatePiecesFromLength(length, standardLength)`**
```javascript
// Штуки = ОКРУГЛЕНИЕ_ВВЕРХ(Длина / СтандартнаяДлина)
return Math.ceil(length / standardLength);
```

**`calculateSheetWeight(width, length, thickness)`**
```javascript
// Формула листов: толщина × ширина × длина × 7.85
const thicknessInMeters = thickness / 1000;
const volumeInM3 = width * length * thicknessInMeters;
return volumeInM3 * STEEL_DENSITY;
```

---

## 💾 БАЗА ДАННЫХ (database/metals.json)

### Структура

```json
{
  "armature_a3": {
    "name": "Арматура А3",
    "category": "Арматура",
    "formula": "round_bar",
    "standardLengths": [6, 11.7, 12],
    "sizes": ["6", "8", "10", "12", ...],
    "coefficients": {
      "6": 0.222,
      "8": 0.395,
      "10": 0.617,
      "12": 0.888,
      ...
    },
    "gost": "ГОСТ 5781-82"
  }
}
```

### Статистика БД

| Параметр | Значение |
|----------|----------|
| Типов металлов | 76 |
| Размеров | 5,704+ |
| Категорий | 21 |
| Размер файла | 457 KB |

### Ключевые типы металла

- **Арматура:** 6 типов (А1, А3, А500С, и т.д.)
- **Крепёж:** 9 типов (болты, гайки, шайбы, гвозди, и т.д.)
- **Балки:** Двутавровые, стальные
- **Швеллеры:** Обычные, специальные
- **Трубы:** Круглые, профильные, бесшовные
- **Листы:** ГК, ХК, оцинкованные
- **Канаты:** Обычные, арматурные, оцинкованные
- **Катанка:** С разными марками стали

---

## 🎨 ПОЛЬЗОВАТЕЛЬСКИЙ ИНТЕРФЕЙС (docs/calculator.html)

### Архитектура UI

**1. Секция выбора металла**
```html
<select id="metal-type">
  <option value="armature_a3">Арматура А3</option>
  <option value="wire_rod">Катанка</option>
  ...
</select>
```

**2. Три взаимозависимых поля**
```html
<input id="input-weight" type="number" placeholder="0.000">
<input id="input-length" type="number" placeholder="0.00">
<input id="input-pieces" type="number" placeholder="0">
```

**Логика взаимозависимости:**
- Пользователь вводит в **любое** поле
- **Debounce 1500ms** (1.5 сек задержка)
- Вызывается `recalculateFields(sourceField)`
- Остальные два поля **автоматически заполняются**

**3. Секция результатов**
```
┌─────────────────────────────────┐
│ ИНФОРМАЦИЯ О МЕТАЛЛЕ (белый)   │
├─────────────────────────────────┤
│ ЗАПРОШЕНО (синий)               │
├─────────────────────────────────┤
│ ФАКТИЧЕСКИ КРАТНО ШТУКАМ (зелёный)│
│ ┌───────────────────────────┐  │
│ │ Длина: 1006.2м [+6.2м]    │  │
│ └───────────────────────────┘  │
└─────────────────────────────────┘
```

### Ключевые функции JavaScript

**`recalculateFields(sourceField)`**
- Определяет какое поле изменил пользователь
- Собирает параметры для `calculateMetal()`
- Вызывает калькулятор
- Заполняет остальные поля

**`displayResult(result)`**
- Формирует HTML результата
- Три секции: Информация / Запрошено / Фактически
- Зелёные бейджи с разницей

**`debounceRecalculate(sourceField, delay)`**
- Задержка 1500ms перед расчётом
- Позволяет вводить многозначные числа

---

## 🧪 СИСТЕМА ТЕСТИРОВАНИЯ

### Типы тестов

**1. Юнит-тесты формул**
- `test-sheet-formula.js` - Формула листов
- `test-no-standard-length.js` - Металлы без standardLength

**2. Интеграционные тесты**
- `test-rounding-fix.js` - Округление кратно штукам
- `test-requested-vs-actual.js` - Структура результата
- `test-fasteners-import.js` - Импорт крепежа

**3. Комплексные тесты**
- `test-final-complete.js` - Полное тестирование (7 тестов)
- `test-pieces-before-fix.js` / `after-fix.js` - Сравнение до/после

### Запуск тестов

```bash
# Все тесты
node test-final-complete.js

# Конкретный тест
node test-rounding-fix.js
```

### Критерии успеха

- ✅ Все тесты должны проходить 100%
- ✅ Арматура: штуки работают (эталон)
- ✅ Катанка/крепёж: штуки работают
- ✅ Округление: всегда вверх
- ✅ Пересчёт: от округлённых штук

---

## 🛠️ ПРОЦЕСС СБОРКИ И ДЕПЛОЯ

### Сборка bundle

**Скрипт:** `build.js`

**Что делает:**
1. Читает `src/calculator.js` и `src/formulas.js`
2. Создаёт два bundle:
   - `dist/calculator.bundle.js` - для Node.js
   - `dist/calculator.browser.js` - для браузера
3. Экспортирует `window.calculateMetal` для браузера

**Команды:**
```bash
node build.js                           # Собрать bundle
cp dist/calculator.browser.js docs/dist/  # Копировать в production
```

### Деплой на GitHub Pages

**Процесс:**
1. Изменения делаются в `src/` или `docs/`
2. Пересборка: `node build.js`
3. Копирование в `docs/`: БД и bundle
4. Git commit + push
5. GitHub Pages обновляется за 1-2 минуты

**Команды:**
```bash
git add src/ database/ docs/ dist/
git commit -m "feat: описание изменений"
git push origin main
```

**Production URL:**
https://lamblackout.github.io/metal-calculator/docs/calculator.html

---

## 🔑 КЛЮЧЕВЫЕ КОНЦЕПЦИИ

### 1. Округление кратно штукам

**Проблема:** Металл отгружается **целыми штуками**, нельзя отдать 0.47 штуки!

**Решение:**
- Всегда округляем **ВВЕРХ** (`Math.ceil`)
- Пересчитываем `length` и `weight` **от округлённых штук**
- Показываем разницу пользователю

### 2. Стандартные длины

**Концепция:**
- Каждый металл имеет `standardLengths: [6, 11.7, 12]`
- Калькулятор выбирает **приоритетную** длину:
  - Арматура: **11.7м** (с завода)
  - Балки/швеллеры: **12м**
  - Катанка/крепёж: **1м по умолчанию** (нет стандарта)

### 3. Взаимозависимые поля

**Принцип:**
- Заполнил **одно** поле → остальные автоматически пересчитываются
- Debounce 1.5 сек → можно вводить многозначные числа
- Нет кнопки "Рассчитать" → всё в реальном времени

### 4. Структура результата

**Три части:**
1. **requested** - что запросил пользователь (1000м)
2. **actual** - что фактически получит (1006.2м кратно штукам)
3. **difference** - разница (+6.2м)

---

## 📊 ПРОИЗВОДИТЕЛЬНОСТЬ

| Параметр | Значение |
|----------|----------|
| Размер БД | 457 KB |
| Bundle (browser) | 23 KB |
| Время расчёта | < 10 ms |
| Debounce UI | 1500 ms |
| Время деплоя | 1-2 мин |

---

## 🔮 БУДУЩЕЕ РАЗВИТИЕ

### Планируется
- [ ] Интеграция с n8n для автоматизации
- [ ] API для внешних систем
- [ ] Мобильная версия
- [ ] Сохранение истории расчётов
- [ ] Экспорт результатов (PDF, Excel)

### Не планируется
- Шпунт, Штакетник, Электроды (по запросу заказчика)
- Немерная длина для всех металлов (только бесшовные трубы)

---

**Автор архитектуры:** Mikhail + Claude (Anthropic)
**Дата создания:** 2025-11-05
**Версия:** 2.0
