// Тесты для Трубы овальной (обычной и оцинкованной)

const { calculateMetal } = require('./src/calculator');
const metalDatabase = require('./docs/database/metals.json');

console.log('═══════════════════════════════════════════════════════════════');
console.log('🧪 ТЕСТЫ ДЛЯ ТРУБЫ ОВАЛЬНОЙ');
console.log('═══════════════════════════════════════════════════════════════\n');

// Функция для тестирования
function testTrybaOval(testName, metalType, size, length, expected) {
  console.log(`📋 ${testName}`);
  console.log('─────────────────────────────────────────────────────────────\n');

  const params = {
    metalType,
    size,
    length
  };

  const result = calculateMetal(params, metalDatabase);

  // Отладка
  if (!result.success) {
    console.log('❌ ОШИБКА:', result.error);
    console.log('   Параметры:', params);
    return false;
  }

  const metal = metalDatabase.metals[metalType];
  const coefficient = metal.weights[size];
  const isGalvanized = metalType === 'tryba_es_oval_ocink';

  console.log('Параметры:');
  console.log(`  Тип: ${metal.name}`);
  console.log(`  Размер: ${size}`);
  console.log(`  Коэффициент: ${coefficient} т/м`);
  console.log(`  Длина: ${length} м`);
  if (isGalvanized) {
    console.log(`  Оцинковка: +3% (×1.03)`);
  }
  console.log();

  console.log('Результат:');
  console.log(`  weightPerMeter (вес 1 м): ${result.weightPerMeter} кг`);
  console.log(`  weight (вес): ${result.weight} т\n`);

  console.log('Расчёт:');
  if (isGalvanized) {
    console.log(`  Формула: вес (т) = коэффициент × длина × 1.03`);
    console.log(`  вес = ${coefficient} × ${length} × 1.03 = ${expected.weight} т`);
  } else {
    console.log(`  Формула: вес (т) = коэффициент × длина`);
    console.log(`  вес = ${coefficient} × ${length} = ${expected.weight} т`);
  }
  console.log();

  const diff = Math.abs(result.weight - expected.weight);

  console.log(`Ожидается: ~${expected.weight} т`);
  console.log(`Получено: ${result.weight} т`);
  console.log(`📊 Статус: ${diff < 0.01 ? '✅ PASSED' : '❌ FAILED'}\n`);

  return diff < 0.01;
}

// ============================================================================
// ТЕСТ 1: Труба овал. 20х10х1 (обычная)
// ============================================================================
// Коэффициент: 0.045732484076 т/м
// Расчёт: 0.045732484076 × 1000 = 45.732484076 т

const test1 = testTrybaOval(
  'ТЕСТ 1: Размер 20х10х1, длина 1000 м (обычная)',
  'tryba_es_oval',
  '20х10х1',
  1000,
  {
    weight: 45.73  // 45.732484076 округляется до 45.73
  }
);

// ============================================================================
// ТЕСТ 2: Труба овал. оц. 20х10х1
// ============================================================================
// Коэффициент: 0.045732484076 т/м
// Расчёт: 0.045732484076 × 1000 × 1.03 = 47.104458598 т

const test2 = testTrybaOval(
  'ТЕСТ 2: Размер 20х10х1, длина 1000 м (оцинкованная)',
  'tryba_es_oval_ocink',
  '20х10х1',
  1000,
  {
    weight: 47.10  // 47.104458598 округляется до 47.10
  }
);

// ============================================================================
// ТЕСТ 3: Труба овал. 50х25х2 (обычная)
// ============================================================================
// Коэффициент: 0.231847133758 т/м
// Расчёт: 0.231847133758 × 500 = 115.923566879 т

const test3 = testTrybaOval(
  'ТЕСТ 3: Размер 50х25х2, длина 500 м (обычная)',
  'tryba_es_oval',
  '50х25х2',
  500,
  {
    weight: 115.92  // 115.923566879 округляется до 115.92
  }
);

// ============================================================================
// ТЕСТ 4: Труба овал. оц. 50х25х2
// ============================================================================
// Коэффициент: 0.231847133758 т/м
// Расчёт: 0.231847133758 × 500 × 1.03 = 119.401273885 т

const test4 = testTrybaOval(
  'ТЕСТ 4: Размер 50х25х2, длина 500 м (оцинкованная)',
  'tryba_es_oval_ocink',
  '50х25х2',
  500,
  {
    weight: 119.40  // 119.401273885 округляется до 119.40
  }
);

// ============================================================================
// ТЕСТ 5: Труба овал. 60х32х1.5 (обычная)
// ============================================================================
// Коэффициент: 0.215286624204 т/м
// Расчёт: 0.215286624204 × 300 = 64.5859872612 т

const test5 = testTrybaOval(
  'ТЕСТ 5: Размер 60х32х1.5, длина 300 м (обычная)',
  'tryba_es_oval',
  '60х32х1.5',
  300,
  {
    weight: 64.59  // 64.5859872612 округляется до 64.59
  }
);

// ============================================================================
// ТЕСТ 6: Труба овал. оц. 60х32х1.5
// ============================================================================
// Коэффициент: 0.215286624204 т/м
// Расчёт: 0.215286624204 × 300 × 1.03 = 66.523566879 т

const test6 = testTrybaOval(
  'ТЕСТ 6: Размер 60х32х1.5, длина 300 м (оцинкованная)',
  'tryba_es_oval_ocink',
  '60х32х1.5',
  300,
  {
    weight: 66.52  // 66.523566879 округляется до 66.52
  }
);

// ============================================================================
// ТЕСТ 7: Малый размер (обычная)
// ============================================================================
// Коэффициент: 0.006535031847 т/м
// Расчёт: 0.006535031847 × 10000 = 65.35031847 т

const test7 = testTrybaOval(
  'ТЕСТ 7: Размер 6х3х0.5, длина 10000 м (обычная)',
  'tryba_es_oval',
  '6х3х0.5',
  10000,
  {
    weight: 65.35  // 65.35031847 округляется до 65.35
  }
);

// ============================================================================
// ТЕСТ 8: Большой размер (оцинк.)
// ============================================================================
// Коэффициент: 0.492993630573 т/м
// Расчёт: 0.492993630573 × 100 × 1.03 = 50.778343949 т

const test8 = testTrybaOval(
  'ТЕСТ 8: Размер 90х32х2.5, длина 100 м (оцинкованная)',
  'tryba_es_oval_ocink',
  '90х32х2.5',
  100,
  {
    weight: 50.78  // 50.778343949 округляется до 50.78
  }
);

// ============================================================================
// ИТОГИ
// ============================================================================

console.log('═══════════════════════════════════════════════════════════════');
console.log('📊 ИТОГИ ТЕСТИРОВАНИЯ');
console.log('═══════════════════════════════════════════════════════════════\n');

const allPassed = test1 && test2 && test3 && test4 && test5 && test6 && test7 && test8;

if (allPassed) {
  console.log('✅ ВСЕ ТЕСТЫ ПРОЙДЕНЫ УСПЕШНО!\n');
  console.log('🎉 Труба овальная работает правильно:');
  console.log('   1. Формула работает: вес = коэффициент × длина');
  console.log('   2. Формула для оцинкованной работает: вес = коэффициент × длина × 1.03');
  console.log('   3. Оцинковка добавляет +3% к весу');
  console.log('   4. Все размеры работают корректно (от малых до больших)');
  console.log('\n📊 Статистика:');
  console.log('   Размеров в базе (обычная): 249');
  console.log('   Размеров в базе (оцинкованная): 249');
  console.log('   ВСЕГО размеров: 498');
  console.log('   Формула: tryba_oval_linear / tryba_oval_galv_linear');
  console.log('   ГОСТ: ГОСТы и ТУ на трубу овальную');
} else {
  console.log('❌ НЕКОТОРЫЕ ТЕСТЫ НЕ ПРОШЛИ!\n');
  console.log(`Тест 1 (20х10х1, обычная): ${test1 ? '✅' : '❌'}`);
  console.log(`Тест 2 (20х10х1, оцинкованная): ${test2 ? '✅' : '❌'}`);
  console.log(`Тест 3 (50х25х2, обычная): ${test3 ? '✅' : '❌'}`);
  console.log(`Тест 4 (50х25х2, оцинкованная): ${test4 ? '✅' : '❌'}`);
  console.log(`Тест 5 (60х32х1.5, обычная): ${test5 ? '✅' : '❌'}`);
  console.log(`Тест 6 (60х32х1.5, оцинкованная): ${test6 ? '✅' : '❌'}`);
  console.log(`Тест 7 (6х3х0.5, обычная): ${test7 ? '✅' : '❌'}`);
  console.log(`Тест 8 (90х32х2.5, оцинкованная): ${test8 ? '✅' : '❌'}`);
}

console.log('\n═══════════════════════════════════════════════════════════════');
