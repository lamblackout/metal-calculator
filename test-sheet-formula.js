// ТЕСТ: Формула расчёта листов
// Проверяет что формула соответствует требованию заказчика:
// Масса = толщина (мм) × ширина (м) × длина (м) × 7.85

const formulas = require('./src/formulas.js');

console.log('\n🧪 ТЕСТ: Формула расчёта листов\n');
console.log('Формула заказчика: Масса = толщина (мм) × ширина (м) × длина (м) × 7.85');
console.log('='.repeat(70));

let passed = 0;
let failed = 0;

// Тест 1: 10мм × 1.5м × 6м = 706.5 кг
console.log('\n📦 ТЕСТ 1: Лист 10мм × 1.5м × 6м\n');

const result1 = formulas.calculateSheetWeight(1.5, 6, 10);
const expected1 = 10 * 1.5 * 6 * 7.85;

console.log(`Расчёт: 10мм × 1.5м × 6м × 7.85 = ${expected1} кг`);
console.log(`Результат функции: ${result1} кг`);

if (Math.abs(result1 - expected1) < 0.01) {
  console.log('✅ ТЕСТ 1 ПРОЙДЕН');
  passed++;
} else {
  console.log(`❌ ТЕСТ 1 ПРОВАЛЕН: ожидалось ${expected1}, получено ${result1}`);
  failed++;
}

console.log('\n' + '='.repeat(70));

// Тест 2: 2мм × 1м × 2м = 31.4 кг
console.log('\n📦 ТЕСТ 2: Лист 2мм × 1м × 2м\n');

const result2 = formulas.calculateSheetWeight(1, 2, 2);
const expected2 = 2 * 1 * 2 * 7.85;

console.log(`Расчёт: 2мм × 1м × 2м × 7.85 = ${expected2} кг`);
console.log(`Результат функции: ${result2} кг`);

if (Math.abs(result2 - expected2) < 0.01) {
  console.log('✅ ТЕСТ 2 ПРОЙДЕН');
  passed++;
} else {
  console.log(`❌ ТЕСТ 2 ПРОВАЛЕН: ожидалось ${expected2}, получено ${result2}`);
  failed++;
}

console.log('\n' + '='.repeat(70));

// Тест 3: 1мм × 1м × 1м = 7.85 кг (базовый коэффициент)
console.log('\n📦 ТЕСТ 3: Лист 1мм × 1м × 1м (проверка коэффициента)\n');

const result3 = formulas.calculateSheetWeight(1, 1, 1);
const expected3 = 1 * 1 * 1 * 7.85;

console.log(`Расчёт: 1мм × 1м × 1м × 7.85 = ${expected3} кг`);
console.log(`Результат функции: ${result3} кг`);

if (Math.abs(result3 - expected3) < 0.01) {
  console.log('✅ ТЕСТ 3 ПРОЙДЕН');
  passed++;
} else {
  console.log(`❌ ТЕСТ 3 ПРОВАЛЕН: ожидалось ${expected3}, получено ${result3}`);
  failed++;
}

console.log('\n' + '='.repeat(70));

// Тест 4: Проверка что STEEL_DENSITY = 7850
console.log('\n📦 ТЕСТ 4: Проверка константы STEEL_DENSITY\n');

// Проверяем через вычисление: если коэффициент правильный, то
// для толщины 1000мм (= 1м) результат должен быть 1м³ × 7850 кг/м³ = 7850 кг
const result4 = formulas.calculateSheetWeight(1, 1, 1000);
const expected4 = 1000 * 1 * 1 * 7.85; // = 7850

console.log(`Проверка: 1000мм × 1м × 1м = ${expected4} кг`);
console.log(`Результат функции: ${result4} кг`);
console.log(`Это подтверждает что STEEL_DENSITY = 7850 кг/м³`);

if (Math.abs(result4 - expected4) < 0.01) {
  console.log('✅ ТЕСТ 4 ПРОЙДЕН');
  passed++;
} else {
  console.log(`❌ ТЕСТ 4 ПРОВАЛЕН: ожидалось ${expected4}, получено ${result4}`);
  failed++;
}

console.log('\n' + '='.repeat(70));

const total = passed + failed;
console.log(`\n📊 ИТОГО: ${passed}/${total} тестов пройдено\n`);

if (failed === 0) {
  console.log('🎉 ВСЕ ТЕСТЫ ПРОЙДЕНЫ!');
  console.log('   ✅ Формула calculateSheetWeight() работает корректно');
  console.log('   ✅ Коэффициент 7.85 применяется правильно');
  console.log('   ✅ Константа STEEL_DENSITY = 7850 кг/м³');
  console.log('   ✅ Формула эквивалентна требованию заказчика\n');
  console.log('🎯 СТАТУС: ФОРМУЛА СООТВЕТСТВУЕТ ТРЕБОВАНИЯМ!');
  process.exit(0);
} else {
  console.log(`⚠️  ${failed} ТЕСТ(ОВ) НЕ ПРОШЛИ!`);
  process.exit(1);
}
