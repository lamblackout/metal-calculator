# 🚨 КРИТИЧЕСКИЕ ОШИБКИ В РЕАЛИЗАЦИИ

## ❌ ПРОБЛЕМА 1: Не считаются штуки по площади

### Что происходит сейчас:
На скриншоте видно:
- Площадь: 100.00 м²
- Ширина: 10 м
- Длина листа: 10 м
- **Количество: 880 шт** ← ЭТО НЕПРАВИЛЬНО!

### Что должно быть:
```javascript
Площадь 1 листа = Ширина × Длина = 10 × 10 = 100 м²
Количество штук = Общая площадь / Площадь 1 листа = 100 / 100 = 1 шт
```

### Почему это критично:
Сейчас система показывает **880 штук** вместо **1 штуки**. Это значит:
- Формула расчёта количества штук **НЕ РАБОТАЕТ**
- Система игнорирует ширину и длину листа

---

## ❌ ПРОБЛЕМА 2: Не пересчитывается при изменении стандарта (Лист ПВ)

### Что происходит:
- Пользователь выбирает размер: 508
- Пользователь выбирает стандарт: ТУ 36.26.11-5-89
- Вводит тонны: 2
- Система показывает: Длина 104.00 м, Количество 104 шт

### Что должно происходить:
1. При выборе **размера 508** → загрузить стандарты из `sizeStandards["508"]`
2. При выборе **стандарта** → взять его коэффициент
3. При вводе **тонн** → пересчитать метры по формуле
4. **При изменении стандарта** → ПЕРЕСЧИТАТЬ метры и количество!

### Сейчас НЕ происходит:
❌ При смене стандарта система НЕ делает пересчёт

---

## ❌ ПРОБЛЕМА 3: Игнорируются размеры 1 листа при вводе тонн

### Что происходит на скриншоте 5:
- Вес: 2 тонны
- Длина: 104.00 м
- Площадь: 1.00 м²
- Ширина: 1 м
- Длина листа: 1 м
- Количество: 104 шт ← НЕПРАВИЛЬНО!

### Что должно быть:
```javascript
// Шаг 1: Рассчитать общую площадь из тонн
Площадь_общая = Тонны × 1000 / (calc_koef1 × stal_koef)

// Шаг 2: Рассчитать площадь 1 листа
Площадь_1_листа = Ширина × Длина_листа = 1 × 1 = 1 м²

// Шаг 3: Рассчитать количество штук
Количество = Площадь_общая / Площадь_1_листа
```

### Почему это критично:
Система **НЕ ИСПОЛЬЗУЕТ** введённые ширину и длину листа для расчёта количества штук!

---

## 🔧 ЧТО НУЖНО ИСПРАВИТЬ

### Исправление 1: Правильный расчёт количества штук

**Было (неправильно):**
```javascript
// Система просто делит метры на что-то непонятное
количество = метры / ???
```

**Должно быть (правильно):**
```javascript
// ДЛЯ ЛИНЕЙНЫХ ТИПОВ (Круг, Лист ПВ)
if (есть_длина_1_шт && длина_1_шт > 0) {
  количество_штук = метры / длина_1_шт
} else {
  количество_штук = null  // не показывать
}

// ДЛЯ ПЛОЩАДНЫХ ТИПОВ (Лента, Лист г/к, х/к и т.д.)
if (есть_ширина_1_шт && ширина_1_шт > 0 && 
    есть_длина_1_шт && длина_1_шт > 0) {
  площадь_1_шт = ширина_1_шт × длина_1_шт
  количество_штук = кв_метры / площадь_1_шт
} else {
  количество_штук = null  // не показывать
}
```

---

### Исправление 2: Пересчёт при изменении любого параметра

**Нужно добавить обработчики событий:**

```javascript
// При изменении ЛЮБОГО параметра → пересчитать ВСЁ

onРазмерChange() {
  // 1. Обновить коэффициент calc_koef1
  // 2. Для Листа ПВ: загрузить новые стандарты
  // 3. Пересчитать всё
  recalculate()
}

onМаркаСталиChange() {
  // 1. Обновить коэффициент stal_koef
  // 2. Пересчитать всё
  recalculate()
}

onСтандартChange() {  // Для Листа ПВ
  // 1. Обновить коэффициент из стандарта
  // 2. Пересчитать всё
  recalculate()
}

onОцинковкаChange() {
  // 1. Обновить коэффициент calc_ocink_koef
  // 2. Пересчитать всё
  recalculate()
}

onТонныChange() {
  // Пересчитать метры/кв.м и количество
  recalculate()
}

onШиринаChange() {
  // Пересчитать количество штук
  recalculate()
}

onДлинаChange() {
  // Пересчитать количество штук
  recalculate()
}

onКоличествоШтукChange() {
  // Пересчитать метры/кв.м и тонны
  recalculate()
}
```

---

### Исправление 3: Универсальная функция recalculate()

```javascript
function recalculate() {
  // Определяем какие поля заполнены
  const есть_тонны = тонны !== null && тонны !== ''
  const есть_количество = количество_штук !== null && количество_штук !== ''
  const есть_размеры_1_шт = проверитьРазмеры1Шт()
  
  // ПРЯМОЙ РАСЧЁТ: Тонны → Метры/Кв.м → Количество
  if (есть_тонны) {
    // Шаг 1: Рассчитать метры или кв.метры
    if (типЛинейный) {
      метры = рассчитатьМетры(тонны, calc_koef1, stal_koef)
    } else {
      кв_метры = рассчитатьКвМетры(тонны, calc_koef1, stal_koef, calc_ocink_koef)
    }
    
    // Шаг 2: Рассчитать количество (если есть размеры 1 шт)
    if (есть_размеры_1_шт) {
      if (типЛинейный) {
        количество_штук = метры / длина_1_шт
      } else {
        площадь_1_шт = ширина_1_шт × длина_1_шт
        количество_штук = кв_метры / площадь_1_шт
      }
    }
  }
  
  // ОБРАТНЫЙ РАСЧЁТ: Количество → Метры/Кв.м → Тонны
  else if (есть_количество && есть_размеры_1_шт) {
    // Шаг 1: Рассчитать метры или кв.метры
    if (типЛинейный) {
      метры = длина_1_шт × количество_штук
    } else {
      площадь_1_шт = ширина_1_шт × длина_1_шт
      кв_метры = площадь_1_шт × количество_штук
    }
    
    // Шаг 2: Рассчитать тонны
    if (типЛинейный) {
      тонны = рассчитатьТонныИзМетров(метры, calc_koef1, stal_koef)
    } else {
      тонны = рассчитатьТонныИзКвМетров(кв_метры, calc_koef1, stal_koef, calc_ocink_koef)
    }
  }
  
  // Обновить UI
  обновитьПоля()
}
```

---

## 📋 КОНКРЕТНЫЕ ПРИМЕРЫ ИСПРАВЛЕНИЙ

### Пример 1: Лист ПВ (скриншот 2)

**Сейчас:**
```
Тонны: 7
Метры: 879.397
Ширина: 10 м
Длина: 10 м
Количество: 8.79 шт  ← НЕПРАВИЛЬНО! Для Листа ПВ это линейный расчёт!
```

**Должно быть:**
```javascript
// Лист ПВ — это ЛИНЕЙНЫЙ тип!
// Размеры листа (ширина × длина) НЕ используются для расчёта площади
// Используется только ДЛИНА листа для расчёта количества

Метры: 879.397 м
Если длина 1 листа = 10 м:
Количество = 879.397 / 10 = 87.9 ≈ 88 листов

// Ширина и "Длина листа" — это РАЗНЫЕ параметры!
// Для Листа ПВ нужно только "Длина 1 листа, м" (без ширины)
```

---

### Пример 2: Площадной тип (скриншот 1)

**Сейчас:**
```
Тонны: 7
Площадь: 100.00 м²
Ширина: 10 м
Длина листа: 10 м
Количество: 880 шт  ← НЕПРАВИЛЬНО!
```

**Должно быть:**
```javascript
Общая площадь: 100 м²
Площадь 1 листа: 10 × 10 = 100 м²
Количество: 100 / 100 = 1 лист
```

---

## ✅ КОНТРОЛЬНЫЕ ТЕСТЫ

После исправления проверь:

### Тест 1: Лист ПВ (линейный)
```
Вход:
- Размер: 404
- Стандарт: ТУ (коэф. 1.014)
- Тонны: 1
- Длина 1 листа: 10 м

Ожидаемый результат:
- Метры: 1000 / (1.014 × 7.85) = 125.6 м
- Количество: 125.6 / 10 = 12.6 ≈ 13 листов
```

### Тест 2: Лента/штрипс (площадной)
```
Вход:
- Размер: 0.5 мм
- Марка стали: Ст3
- Тонны: 1
- Ширина: 1 м
- Длина: 2 м

Ожидаемый результат:
- Кв.метры: 1000 / (0.5 × 7.85) = 254.78 м²
- Площадь 1 шт: 1 × 2 = 2 м²
- Количество: 254.78 / 2 = 127 шт
```

### Тест 3: Изменение стандарта
```
Действия:
1. Выбрать Лист ПВ, размер 406, стандарт "ТУ 36.26.11-5-89" (коэф. 2)
2. Ввести тонны: 1
3. Проверить метры: должно быть 1000 / (2 × 7.85) = 63.69 м
4. СМЕНИТЬ стандарт на "ГОСТ 8706-78" (коэф. тоже 2)
5. ✅ Метры должны ПЕРЕСЧИТАТЬСЯ (остаться те же, т.к. коэф. одинаковый)
```

### Тест 4: Изменение количества штук (обратный расчёт)
```
Действия:
1. Выбрать Лента/штрипс, размер 0.5 мм, Ст3
2. Ввести ширину: 1 м, длину: 2 м
3. Ввести количество: 100 шт
4. ✅ Система должна рассчитать:
   - Площадь: 1 × 2 × 100 = 200 м²
   - Тонны: 0.5 × 200 × 7.85 / 1000 = 0.785 т
```

---

## 🎯 ГЛАВНОЕ

1. **Количество штук рассчитывается по площади ДЛЯ ПЛОЩАДНЫХ типов**
   - Площадь 1 листа = Ширина × Длина
   - Количество = Общая площадь / Площадь 1 листа

2. **Количество штук рассчитывается по длине ДЛЯ ЛИНЕЙНЫХ типов**
   - Количество = Общие метры / Длина 1 листа

3. **При изменении ЛЮБОГО параметра → всё пересчитывается**
   - Размер → пересчёт
   - Стандарт → пересчёт
   - Марка стали → пересчёт
   - Оцинковка → пересчёт
   - Тонны → пересчёт
   - Ширина/Длина → пересчёт
   - Количество → пересчёт

4. **Расчёты работают В ОБА НАПРАВЛЕНИЯ**
   - Тонны → Количество (прямой)
   - Количество → Тонны (обратный)

---

## 📝 ЧТО ОТПРАВИТЬ CLAUDE CODE

Отправь ему этот файл + скажи:

```
КРИТИЧЕСКИЕ ОШИБКИ в текущей реализации:

1. Количество штук НЕ СЧИТАЕТСЯ по площади (см. скриншот 1)
   - Показывает 880 вместо 1
   - Игнорирует ширину и длину листа

2. НЕ ПЕРЕСЧИТЫВАЕТСЯ при изменении стандарта (Лист ПВ)
   - При смене стандарта метры должны пересчитываться

3. ИГНОРИРУЮТСЯ размеры листа при вводе тонн
   - Вводишь ширину 1м и длину 1м → количество считается неправильно

НУЖНО ИСПРАВИТЬ СРОЧНО:
- Добавить правильный расчёт количества штук (см. раздел "Исправление 1")
- Добавить пересчёт при изменении параметров (см. "Исправление 2")
- Реализовать универсальную функцию recalculate() (см. "Исправление 3")

Проверь по контрольным тестам после исправления!
```
