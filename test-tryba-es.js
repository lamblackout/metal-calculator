// Тесты для Трубы электросварной (обычной и оцинкованной)

const { calculateMetal } = require('./src/calculator');
const metalDatabase = require('./docs/database/metals.json');

console.log('═══════════════════════════════════════════════════════════════');
console.log('🧪 ТЕСТЫ ДЛЯ ТРУБЫ ЭЛЕКТРОСВАРНОЙ');
console.log('═══════════════════════════════════════════════════════════════\n');

// Функция для тестирования
function testTrybaEs(testName, metalType, size, length, expected) {
  console.log(`📋 ${testName}`);
  console.log('─────────────────────────────────────────────────────────────\n');

  const params = {
    metalType,
    size,
    length
  };

  const result = calculateMetal(params, metalDatabase);

  // Отладка
  if (!result.success) {
    console.log('❌ ОШИБКА:', result.error);
    console.log('   Параметры:', params);
    return false;
  }

  const metal = metalDatabase.metals[metalType];
  const coefficient = metal.weights[size];
  const isGalvanized = metalType === 'tryba_es_ocink';

  console.log('Параметры:');
  console.log(`  Тип: ${metal.name}`);
  console.log(`  Размер: ${size}`);
  console.log(`  Коэффициент: ${coefficient}`);
  console.log(`  Длина: ${length} м`);
  if (isGalvanized) {
    console.log(`  Оцинковка: +3% (×1.03)`);
  }
  console.log();

  console.log('Результат:');
  console.log(`  weightPerMeter (вес 1 м): ${result.weightPerMeter} кг`);
  console.log(`  weight (вес): ${result.weight} т\n`);

  console.log('Расчёт:');
  if (isGalvanized) {
    console.log(`  Формула: вес (т) = (коэффициент × длина × 7.85 × 1.03) / 1000`);
    console.log(`  вес = (${coefficient} × ${length} × 7.85 × 1.03) / 1000 = ${expected.weight} т`);
  } else {
    console.log(`  Формула: вес (т) = (коэффициент × длина × 7.85) / 1000`);
    console.log(`  вес = (${coefficient} × ${length} × 7.85) / 1000 = ${expected.weight} т`);
  }
  console.log();

  const diff = Math.abs(result.weight - expected.weight);

  console.log(`Ожидается: ~${expected.weight} т`);
  console.log(`Получено: ${result.weight} т`);
  console.log(`📊 Статус: ${diff < 0.01 ? '✅ PASSED' : '❌ FAILED'}\n`);

  return diff < 0.01;
}

// ============================================================================
// ТЕСТ 1: Малый размер (10×1) - ОБЫЧНАЯ
// ============================================================================
// Коэффициент: 0.02827430828
// Расчёт: (0.02827430828 × 1000 × 7.85) / 1000 = 0.22195 т

const test1 = testTrybaEs(
  'ТЕСТ 1: Размер 10×1, длина 1000 м (обычная)',
  'tryba_es',
  '10х1',
  1000,
  {
    weight: 0.22  // 0.22195 округляется до 0.22
  }
);

// ============================================================================
// ТЕСТ 2: Малый размер (10×1) - ОЦИНКОВАННАЯ
// ============================================================================
// Коэффициент: 0.02827430828
// Расчёт: (0.02827430828 × 1000 × 7.85 × 1.03) / 1000 = 0.22861 т

const test2 = testTrybaEs(
  'ТЕСТ 2: Размер 10×1, длина 1000 м (оцинкованная)',
  'tryba_es_ocink',
  '10х1',
  1000,
  {
    weight: 0.23  // 0.22861 округляется до 0.23
  }
);

// ============================================================================
// ТЕСТ 3: Средний размер (89×4) - ОБЫЧНАЯ
// ============================================================================
// Коэффициент: 1.068140535032
// Расчёт: (1.068140535032 × 500 × 7.85) / 1000 = 4.19295 т

const test3 = testTrybaEs(
  'ТЕСТ 3: Размер 89×4, длина 500 м (обычная)',
  'tryba_es',
  '89х4',
  500,
  {
    weight: 4.19  // 4.19295 округляется до 4.19
  }
);

// ============================================================================
// ТЕСТ 4: Средний размер (89×4) - ОЦИНКОВАННАЯ
// ============================================================================
// Коэффициент: 1.068140535032
// Расчёт: (1.068140535032 × 500 × 7.85 × 1.03) / 1000 = 4.31874 т

const test4 = testTrybaEs(
  'ТЕСТ 4: Размер 89×4, длина 500 м (оцинкованная)',
  'tryba_es_ocink',
  '89х4',
  500,
  {
    weight: 4.32  // 4.31874 округляется до 4.32
  }
);

// ============================================================================
// ТЕСТ 5: Большой размер (273×10) - ОБЫЧНАЯ
// ============================================================================
// Коэффициент: 8.262381197452
// Расчёт: (8.262381197452 × 250 × 7.85) / 1000 = 16.21593 т

const test5 = testTrybaEs(
  'ТЕСТ 5: Размер 273×10, длина 250 м (обычная)',
  'tryba_es',
  '273х10',
  250,
  {
    weight: 16.22  // 16.21593 округляется до 16.22
  }
);

// ============================================================================
// ТЕСТ 6: Большой размер (273×10) - ОЦИНКОВАННАЯ
// ============================================================================
// Коэффициент: 8.262381197452
// Расчёт: (8.262381197452 × 250 × 7.85 × 1.03) / 1000 = 16.70241 т

const test6 = testTrybaEs(
  'ТЕСТ 6: Размер 273×10, длина 250 м (оцинкованная)',
  'tryba_es_ocink',
  '273х10',
  250,
  {
    weight: 16.70  // 16.70241 округляется до 16.70
  }
);

// ============================================================================
// ТЕСТ 7: Очень большой размер (1020×12) - ОБЫЧНАЯ
// ============================================================================
// Коэффициент: 38.000670328662
// Расчёт: (38.000670328662 × 100 × 7.85) / 1000 = 29.83053 т

const test7 = testTrybaEs(
  'ТЕСТ 7: Размер 1020×12, длина 100 м (обычная)',
  'tryba_es',
  '1020х12',
  100,
  {
    weight: 29.83  // 29.83053 округляется до 29.83
  }
);

// ============================================================================
// ТЕСТ 8: Очень большой размер (1020×12) - ОЦИНКОВАННАЯ
// ============================================================================
// Коэффициент: 38.000670328662
// Расчёт: (38.000670328662 × 100 × 7.85 × 1.03) / 1000 = 30.72544 т

const test8 = testTrybaEs(
  'ТЕСТ 8: Размер 1020×12, длина 100 м (оцинкованная)',
  'tryba_es_ocink',
  '1020х12',
  100,
  {
    weight: 30.73  // 30.72544 округляется до 30.73
  }
);

// ============================================================================
// ТЕСТ 9: Максимальный размер (4520×50) - ОБЫЧНАЯ
// ============================================================================
// Коэффициент: 702.14532229299
// Расчёт: (702.14532229299 × 10 × 7.85) / 1000 = 55.11835 т

const test9 = testTrybaEs(
  'ТЕСТ 9: Размер 4520×50, длина 10 м (обычная)',
  'tryba_es',
  '4520х50',
  10,
  {
    weight: 55.12  // 55.11835 округляется до 55.12
  }
);

// ============================================================================
// ТЕСТ 10: Максимальный размер (4520×50) - ОЦИНКОВАННАЯ
// ============================================================================
// Коэффициент: 702.14532229299
// Расчёт: (702.14532229299 × 10 × 7.85 × 1.03) / 1000 = 56.77190 т

const test10 = testTrybaEs(
  'ТЕСТ 10: Размер 4520×50, длина 10 м (оцинкованная)',
  'tryba_es_ocink',
  '4520х50',
  10,
  {
    weight: 56.77  // 56.77190 округляется до 56.77
  }
);

// ============================================================================
// ИТОГИ
// ============================================================================

console.log('═══════════════════════════════════════════════════════════════');
console.log('📊 ИТОГИ ТЕСТИРОВАНИЯ');
console.log('═══════════════════════════════════════════════════════════════\n');

const allPassed = test1 && test2 && test3 && test4 && test5 && test6 && test7 && test8 && test9 && test10;

if (allPassed) {
  console.log('✅ ВСЕ ТЕСТЫ ПРОЙДЕНЫ УСПЕШНО!\n');
  console.log('🎉 Труба электросварная работает правильно:');
  console.log('   1. Формула работает: (коэффициент × длина × 7.85) / 1000');
  console.log('   2. Формула для оцинкованной работает: (коэффициент × длина × 7.85 × 1.03) / 1000');
  console.log('   3. Оцинковка добавляет +3% к весу');
  console.log('   4. Все размеры работают корректно (от малых до максимальных)');
  console.log('   5. Мусорные записи (users_size, ────) успешно удалены');
  console.log('\n📊 Статистика:');
  console.log('   Размеров в базе (обычная): 3530');
  console.log('   Размеров в базе (оцинкованная): 3530');
  console.log('   ВСЕГО размеров: 7060');
  console.log('   Формула: tryba_es_linear / tryba_es_galv_linear');
  console.log('   ГОСТ: ГОСТы и ТУ на трубу электросварную');
} else {
  console.log('❌ НЕКОТОРЫЕ ТЕСТЫ НЕ ПРОШЛИ!\n');
  console.log(`Тест 1 (10×1, обычная): ${test1 ? '✅' : '❌'}`);
  console.log(`Тест 2 (10×1, оцинкованная): ${test2 ? '✅' : '❌'}`);
  console.log(`Тест 3 (89×4, обычная): ${test3 ? '✅' : '❌'}`);
  console.log(`Тест 4 (89×4, оцинкованная): ${test4 ? '✅' : '❌'}`);
  console.log(`Тест 5 (273×10, обычная): ${test5 ? '✅' : '❌'}`);
  console.log(`Тест 6 (273×10, оцинкованная): ${test6 ? '✅' : '❌'}`);
  console.log(`Тест 7 (1020×12, обычная): ${test7 ? '✅' : '❌'}`);
  console.log(`Тест 8 (1020×12, оцинкованная): ${test8 ? '✅' : '❌'}`);
  console.log(`Тест 9 (4520×50, обычная): ${test9 ? '✅' : '❌'}`);
  console.log(`Тест 10 (4520×50, оцинкованная): ${test10 ? '✅' : '❌'}`);
}

console.log('\n═══════════════════════════════════════════════════════════════');
