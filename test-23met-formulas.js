// Тесты для проверки исправленных формул
// Эталонные данные с 23met.ru

const { calculateMetal } = require('./src/calculator');
const metalDatabase = require('./docs/database/metals.json');

console.log('═══════════════════════════════════════════════════════════════');
console.log('🧪 ТЕСТЫ ПО ЭТАЛОННЫМ ДАННЫМ С 23MET.RU');
console.log('═══════════════════════════════════════════════════════════════\n');

let allTestsPassed = true;

// ═══════════════════════════════════════════════════════════════
// ТЕСТ 1: Лента БЕЗ оцинковки — влияние марки стали
// ═══════════════════════════════════════════════════════════════
console.log('📋 ТЕСТ 1: Влияние марки стали (БЕЗ оцинковки)');
console.log('─────────────────────────────────────────────────────────────\n');

// Тест 1.1: Сталь ст3 (7.85)
const test1_1 = calculateMetal({
  metalType: 'strip_tape_painted',
  size: '1',
  area: 300,
  steelType: 'ст3',
  zincOption: 'нет'
}, metalDatabase);

console.log('Тест 1.1: Лента 1мм, 300м², сталь ст3 (7.85), без оцинковки');
console.log(`  Результат: ${test1_1.weight} т`);
console.log(`  Эталон:    2.355 т`);
console.log(`  Статус:    ${Math.abs(test1_1.weight - 2.355) < 0.001 ? '✅ PASSED' : '❌ FAILED'}`);

if (Math.abs(test1_1.weight - 2.355) >= 0.001) {
  allTestsPassed = false;
  console.log(`  ⚠️  Ошибка: ${(test1_1.weight - 2.355).toFixed(3)} т`);
}
console.log();

// Тест 1.2: Сталь ст45 (7.826)
const test1_2 = calculateMetal({
  metalType: 'strip_tape_painted',
  size: '1',
  area: 300,
  steelType: 'ст45',
  zincOption: 'нет'
}, metalDatabase);

console.log('Тест 1.2: Лента 1мм, 300м², сталь ст45 (7.826), без оцинковки');
console.log(`  Результат: ${test1_2.weight} т`);
console.log(`  Эталон:    2.348 т`);
console.log(`  Статус:    ${Math.abs(test1_2.weight - 2.348) < 0.001 ? '✅ PASSED' : '❌ FAILED'}`);

if (Math.abs(test1_2.weight - 2.348) >= 0.001) {
  allTestsPassed = false;
  console.log(`  ⚠️  Ошибка: ${(test1_2.weight - 2.348).toFixed(3)} т`);
}
console.log();

// Проверка разницы между ст3 и ст45
const diff1 = test1_2.weight - test1_1.weight;
console.log(`📊 Разница ст45 - ст3: ${diff1.toFixed(3)} т`);
console.log(`   Ожидается: -0.007 т (ст45 легче на 0.3%)`);
console.log(`   Статус:    ${Math.abs(diff1 - (-0.007)) < 0.001 ? '✅ PASSED' : '❌ FAILED'}`);

if (Math.abs(diff1 - (-0.007)) >= 0.001) {
  allTestsPassed = false;
}
console.log();

// ═══════════════════════════════════════════════════════════════
// ТЕСТ 2: Лента С оцинковкой — правильная формула
// ═══════════════════════════════════════════════════════════════
console.log('─────────────────────────────────────────────────────────────');
console.log('📋 ТЕСТ 2: Влияние оцинковки (правильная формула)');
console.log('─────────────────────────────────────────────────────────────\n');

// Тест 2.1: БЕЗ оцинковки
const test2_1 = calculateMetal({
  metalType: 'strip_tape_painted',
  size: '1',
  area: 300,
  steelType: 'ст3',
  zincOption: 'нет'
}, metalDatabase);

console.log('Тест 2.1: Лента 1мм, 300м², сталь ст3 (7.85), БЕЗ оцинковки');
console.log(`  Результат: ${test2_1.weight} т`);
console.log(`  Эталон:    2.355 т`);
console.log(`  Статус:    ${Math.abs(test2_1.weight - 2.355) < 0.001 ? '✅ PASSED' : '❌ FAILED'}`);

if (Math.abs(test2_1.weight - 2.355) >= 0.001) {
  allTestsPassed = false;
  console.log(`  ⚠️  Ошибка: ${(test2_1.weight - 2.355).toFixed(3)} т`);
}
console.log();

// Тест 2.2: С оцинковкой 190 г/м²
const test2_2 = calculateMetal({
  metalType: 'strip_tape_painted',
  size: '1',
  area: 300,
  steelType: 'ст3',
  zincOption: '190 г/м²'
}, metalDatabase);

console.log('Тест 2.2: Лента 1мм, 300м², сталь ст3 (7.85), оцинковка 190 г/м²');
console.log(`  Результат: ${test2_2.weight} т`);
console.log(`  Эталон:    2.412 т`);
console.log(`  Статус:    ${Math.abs(test2_2.weight - 2.412) < 0.001 ? '✅ PASSED' : '❌ FAILED'}`);

if (Math.abs(test2_2.weight - 2.412) >= 0.001) {
  allTestsPassed = false;
  console.log(`  ⚠️  Ошибка: ${(test2_2.weight - 2.412).toFixed(3)} т`);
}
console.log();

// Проверка разницы оцинковки
const diff2 = test2_2.weight - test2_1.weight;
console.log(`📊 Разница с/без оцинковки: +${diff2.toFixed(3)} т`);
console.log(`   Ожидается: +0.057 т`);
console.log(`   Статус:    ${Math.abs(diff2 - 0.057) < 0.001 ? '✅ PASSED' : '❌ FAILED'}`);

if (Math.abs(diff2 - 0.057) >= 0.001) {
  allTestsPassed = false;
}
console.log();

// ═══════════════════════════════════════════════════════════════
// ТЕСТ 3: Лента С оцинковкой — влияние марки стали
// ═══════════════════════════════════════════════════════════════
console.log('─────────────────────────────────────────────────────────────');
console.log('📋 ТЕСТ 3: Влияние марки стали С оцинковкой');
console.log('─────────────────────────────────────────────────────────────\n');

// Тест 3.1: Сталь ст3 с оцинковкой
const test3_1 = calculateMetal({
  metalType: 'strip_tape_painted',
  size: '1',
  area: 300,
  steelType: 'ст3',
  zincOption: '190 г/м²'
}, metalDatabase);

console.log('Тест 3.1: Лента 1мм, 300м², сталь ст3 (7.85), оцинковка 190 г/м²');
console.log(`  Результат: ${test3_1.weight} т`);
console.log(`  Эталон:    2.412 т`);
console.log(`  Статус:    ${Math.abs(test3_1.weight - 2.412) < 0.001 ? '✅ PASSED' : '❌ FAILED'}`);

if (Math.abs(test3_1.weight - 2.412) >= 0.001) {
  allTestsPassed = false;
  console.log(`  ⚠️  Ошибка: ${(test3_1.weight - 2.412).toFixed(3)} т`);
}
console.log();

// Тест 3.2: Сталь ст45 с оцинковкой
const test3_2 = calculateMetal({
  metalType: 'strip_tape_painted',
  size: '1',
  area: 300,
  steelType: 'ст45',
  zincOption: '190 г/м²'
}, metalDatabase);

console.log('Тест 3.2: Лента 1мм, 300м², сталь ст45 (7.826), оцинковка 190 г/м²');
console.log(`  Результат: ${test3_2.weight} т`);
console.log(`  Эталон:    2.405 т (расчётное)`);
console.log(`  Расчёт:    (1 × 7.826 + 0.19) × 300 / 1000 = 2.405 т`);
console.log(`  Статус:    ${Math.abs(test3_2.weight - 2.405) < 0.001 ? '✅ PASSED' : '❌ FAILED'}`);

if (Math.abs(test3_2.weight - 2.405) >= 0.001) {
  allTestsPassed = false;
  console.log(`  ⚠️  Ошибка: ${(test3_2.weight - 2.405).toFixed(3)} т`);
}
console.log();

// Проверка разницы между ст3 и ст45 с оцинковкой
const diff3 = test3_2.weight - test3_1.weight;
console.log(`📊 Разница ст45 - ст3 (с оцинковкой): ${diff3.toFixed(3)} т`);
console.log(`   Ожидается: -0.007 т (та же разница что и без оцинковки)`);
console.log(`   Статус:    ${Math.abs(diff3 - (-0.007)) < 0.001 ? '✅ PASSED' : '❌ FAILED'}`);

if (Math.abs(diff3 - (-0.007)) >= 0.001) {
  allTestsPassed = false;
}
console.log();

// ═══════════════════════════════════════════════════════════════
// ИТОГИ
// ═══════════════════════════════════════════════════════════════
console.log('═══════════════════════════════════════════════════════════════');
console.log('📊 ИТОГИ ТЕСТИРОВАНИЯ');
console.log('═══════════════════════════════════════════════════════════════\n');

if (allTestsPassed) {
  console.log('✅ ВСЕ ТЕСТЫ ПРОЙДЕНЫ УСПЕШНО!');
  console.log('\n🎉 Формулы исправлены правильно:');
  console.log('   1. Марка стали теперь влияет на расчёт веса');
  console.log('   2. Оцинковка считается правильно: (толщина × плотность + оцинковка)');
  console.log('\n✅ Результаты совпадают с эталонными данными 23met.ru');
} else {
  console.log('❌ НЕКОТОРЫЕ ТЕСТЫ НЕ ПРОШЛИ!');
  console.log('\n⚠️  Проверьте формулы расчёта в src/calculator.js');
  process.exit(1);
}

console.log('\n═══════════════════════════════════════════════════════════════\n');
