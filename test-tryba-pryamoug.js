// Тесты для Трубы прямоугольной (обычной и оцинкованной)

const { calculateMetal } = require('./src/calculator');
const metalDatabase = require('./docs/database/metals.json');

console.log('═══════════════════════════════════════════════════════════════');
console.log('🧪 ТЕСТЫ ДЛЯ ТРУБЫ ПРЯМОУГОЛЬНОЙ');
console.log('═══════════════════════════════════════════════════════════════\n');

// Функция для тестирования
function testTrybaPryamoug(testName, metalType, size, length, expected) {
  console.log(`📋 ${testName}`);
  console.log('─────────────────────────────────────────────────────────────\n');

  const params = {
    metalType,
    size,
    length
  };

  const result = calculateMetal(params, metalDatabase);

  // Отладка
  if (!result.success) {
    console.log('❌ ОШИБКА:', result.error);
    console.log('   Параметры:', params);
    return false;
  }

  const metal = metalDatabase.metals[metalType];
  const coefficient = metal.weights[size];
  const isGalvanized = metalType === 'tryba_es_pr_ocink';

  console.log('Параметры:');
  console.log(`  Тип: ${metal.name}`);
  console.log(`  Размер: ${size}`);
  console.log(`  Коэффициент: ${coefficient}`);
  console.log(`  Длина: ${length} м`);
  if (isGalvanized) {
    console.log(`  Оцинковка: +3% (×1.03)`);
  }
  console.log();

  console.log('Результат:');
  console.log(`  weightPerMeter (вес 1 м): ${result.weightPerMeter} кг`);
  console.log(`  weight (вес): ${result.weight} т\n`);

  console.log('Расчёт:');
  if (isGalvanized) {
    console.log(`  Формула: вес (т) = (коэффициент × длина × 7.85 × 1.03) / 1000`);
    console.log(`  вес = (${coefficient} × ${length} × 7.85 × 1.03) / 1000 = ${expected.weight} т`);
  } else {
    console.log(`  Формула: вес (т) = (коэффициент × длина × 7.85) / 1000`);
    console.log(`  вес = (${coefficient} × ${length} × 7.85) / 1000 = ${expected.weight} т`);
  }
  console.log();

  const diff = Math.abs(result.weight - expected.weight);

  console.log(`Ожидается: ~${expected.weight} т`);
  console.log(`Получено: ${result.weight} т`);
  console.log(`📊 Статус: ${diff < 0.01 ? '✅ PASSED' : '❌ FAILED'}\n`);

  return diff < 0.01;
}

// ============================================================================
// ТЕСТ 1: Малый размер (15×10×1) - ОБЫЧНАЯ
// ============================================================================
// Коэффициент: 0.044331210191
// Расчёт: (0.044331210191 × 1000 × 7.85) / 1000 = 0.348 т

const test1 = testTrybaPryamoug(
  'ТЕСТ 1: Размер 15×10×1, длина 1000 м (обычная)',
  'tryba_es_pr',
  '15х10х1',
  1000,
  {
    weight: 0.35  // 0.348 округляется до 0.35
  }
);

// ============================================================================
// ТЕСТ 2: Малый размер (15×10×1) - ОЦИНКОВАННАЯ
// ============================================================================
// Коэффициент: 0.044331210191
// Расчёт: (0.044331210191 × 1000 × 7.85 × 1.03) / 1000 = 0.358 т

const test2 = testTrybaPryamoug(
  'ТЕСТ 2: Размер 15×10×1, длина 1000 м (оцинкованная)',
  'tryba_es_pr_ocink',
  '15х10х1',
  1000,
  {
    weight: 0.36  // 0.358 округляется до 0.36
  }
);

// ============================================================================
// ТЕСТ 3: Средний размер (120×80×5) - ОБЫЧНАЯ
// ============================================================================
// Коэффициент: 1.857324840764
// Расчёт: (1.857324840764 × 250 × 7.85) / 1000 = 3.648 т

const test3 = testTrybaPryamoug(
  'ТЕСТ 3: Размер 120×80×5, длина 250 м (обычная)',
  'tryba_es_pr',
  '120х80х5',
  250,
  {
    weight: 3.65  // 3.648 округляется до 3.65
  }
);

// ============================================================================
// ТЕСТ 4: Средний размер (120×80×5) - ОЦИНКОВАННАЯ
// ============================================================================
// Коэффициент: 1.857324840764
// Расчёт: (1.857324840764 × 250 × 7.85 × 1.03) / 1000 = 3.757 т

const test4 = testTrybaPryamoug(
  'ТЕСТ 4: Размер 120×80×5, длина 250 м (оцинкованная)',
  'tryba_es_pr_ocink',
  '120х80х5',
  250,
  {
    weight: 3.76  // 3.757 округляется до 3.76
  }
);

// ============================================================================
// ТЕСТ 5: Большой размер (300×200×10) - ОБЫЧНАЯ
// ============================================================================
// Коэффициент: 9.256050955414
// Расчёт: (9.256050955414 × 50 × 7.85) / 1000 = 3.633 т

const test5 = testTrybaPryamoug(
  'ТЕСТ 5: Размер 300×200×10, длина 50 м (обычная)',
  'tryba_es_pr',
  '300х200х10',
  50,
  {
    weight: 3.63  // 3.633 округляется до 3.63
  }
);

// ============================================================================
// ТЕСТ 6: Большой размер (300×200×10) - ОЦИНКОВАННАЯ
// ============================================================================
// Коэффициент: 9.256050955414
// Расчёт: (9.256050955414 × 50 × 7.85 × 1.03) / 1000 = 3.741 т

const test6 = testTrybaPryamoug(
  'ТЕСТ 6: Размер 300×200×10, длина 50 м (оцинкованная)',
  'tryba_es_pr_ocink',
  '300х200х10',
  50,
  {
    weight: 3.74  // 3.741 округляется до 3.74
  }
);

// ============================================================================
// ТЕСТ 7: Максимальный размер (700×300×16) - ОБЫЧНАЯ
// ============================================================================
// Коэффициент: 30.53568
// Расчёт: (30.53568 × 10 × 7.85) / 1000 = 2.397 т

const test7 = testTrybaPryamoug(
  'ТЕСТ 7: Размер 700×300×16, длина 10 м (обычная)',
  'tryba_es_pr',
  '700х300х16',
  10,
  {
    weight: 2.40  // 2.397 округляется до 2.40
  }
);

// ============================================================================
// ТЕСТ 8: Максимальный размер (700×300×16) - ОЦИНКОВАННАЯ
// ============================================================================
// Коэффициент: 30.53568
// Расчёт: (30.53568 × 10 × 7.85 × 1.03) / 1000 = 2.469 т

const test8 = testTrybaPryamoug(
  'ТЕСТ 8: Размер 700×300×16, длина 10 м (оцинкованная)',
  'tryba_es_pr_ocink',
  '700х300х16',
  10,
  {
    weight: 2.47  // 2.469 округляется до 2.47
  }
);

// ============================================================================
// ТЕСТ 9: Дробная длина (180×140×7) - ОБЫЧНАЯ
// ============================================================================
// Коэффициент: 4.115923566879
// Расчёт: (4.115923566879 × 37.5 × 7.85) / 1000 = 1.212 т

const test9 = testTrybaPryamoug(
  'ТЕСТ 9: Размер 180×140×7, длина 37.5 м (обычная)',
  'tryba_es_pr',
  '180х140х7',
  37.5,
  {
    weight: 1.21  // 1.212 округляется до 1.21
  }
);

// ============================================================================
// ТЕСТ 10: Дробная длина (180×140×7) - ОЦИНКОВАННАЯ
// ============================================================================
// Коэффициент: 4.115923566879
// Расчёт: (4.115923566879 × 37.5 × 7.85 × 1.03) / 1000 = 1.248 т

const test10 = testTrybaPryamoug(
  'ТЕСТ 10: Размер 180×140×7, длина 37.5 м (оцинкованная)',
  'tryba_es_pr_ocink',
  '180х140х7',
  37.5,
  {
    weight: 1.25  // 1.248 округляется до 1.25
  }
);

// ============================================================================
// ИТОГИ
// ============================================================================

console.log('═══════════════════════════════════════════════════════════════');
console.log('📊 ИТОГИ ТЕСТИРОВАНИЯ');
console.log('═══════════════════════════════════════════════════════════════\n');

const allPassed = test1 && test2 && test3 && test4 && test5 && test6 && test7 && test8 && test9 && test10;

if (allPassed) {
  console.log('✅ ВСЕ ТЕСТЫ ПРОЙДЕНЫ УСПЕШНО!\n');
  console.log('🎉 Труба прямоугольная работает правильно:');
  console.log('   1. Формула работает: (коэффициент × длина × 7.85) / 1000');
  console.log('   2. Формула для оцинкованной работает: (коэффициент × длина × 7.85 × 1.03) / 1000');
  console.log('   3. Оцинковка добавляет +3% к весу');
  console.log('   4. Все размеры работают корректно (от малых до максимальных)');
  console.log('   5. Дробная длина обрабатывается правильно');
  console.log('\n📊 Статистика:');
  console.log('   Размеров в базе (обычная): 932');
  console.log('   Размеров в базе (оцинкованная): 932');
  console.log('   ВСЕГО размеров: 1864');
  console.log('   Формула: tryba_pr_linear / tryba_pr_galv_linear');
  console.log('   ГОСТ: ГОСТы и ТУ на трубу прямоугольную');
} else {
  console.log('❌ НЕКОТОРЫЕ ТЕСТЫ НЕ ПРОШЛИ!\n');
  console.log(`Тест 1 (15×10×1, обычная): ${test1 ? '✅' : '❌'}`);
  console.log(`Тест 2 (15×10×1, оцинкованная): ${test2 ? '✅' : '❌'}`);
  console.log(`Тест 3 (120×80×5, обычная): ${test3 ? '✅' : '❌'}`);
  console.log(`Тест 4 (120×80×5, оцинкованная): ${test4 ? '✅' : '❌'}`);
  console.log(`Тест 5 (300×200×10, обычная): ${test5 ? '✅' : '❌'}`);
  console.log(`Тест 6 (300×200×10, оцинкованная): ${test6 ? '✅' : '❌'}`);
  console.log(`Тест 7 (700×300×16, обычная): ${test7 ? '✅' : '❌'}`);
  console.log(`Тест 8 (700×300×16, оцинкованная): ${test8 ? '✅' : '❌'}`);
  console.log(`Тест 9 (180×140×7, обычная): ${test9 ? '✅' : '❌'}`);
  console.log(`Тест 10 (180×140×7, оцинкованная): ${test10 ? '✅' : '❌'}`);
}

console.log('\n═══════════════════════════════════════════════════════════════');
