// Тесты для Полосы

const { calculateMetal } = require('./src/calculator');
const metalDatabase = require('./docs/database/metals.json');

console.log('═══════════════════════════════════════════════════════════════');
console.log('🧪 ТЕСТЫ ДЛЯ ПОЛОСЫ');
console.log('═══════════════════════════════════════════════════════════════\n');

// ============================================================================
// ТЕСТ 1: Полоса 10×50, длина 10м, ст3
// ============================================================================

console.log('📋 ТЕСТ 1: Полоса 10×50, длина 10м, ст3');
console.log('─────────────────────────────────────────────────────────────\n');

const test1 = calculateMetal({
  metalType: 'strip',
  size: '10х50',
  length: 10,
  steelType: 'ст3'
}, metalDatabase);

console.log('Параметры:');
console.log('  Размер: 10×50');
console.log('  Длина: 10 м');
console.log('  Марка стали: ст3 (7.85 т/м³)');
console.log('  Коэффициент: 0.5 кг/м\n');

console.log('Результат:');
console.log(`  weightPerMeter (вес 1 м): ${test1.weightPerMeter} кг`);
console.log(`  weight (вес): ${test1.weight} т\n`);

console.log('Расчёт:');
console.log('  вес = 0.5 × 10 × 7.85 = 39.25 кг = 0.03925 т\n');

const expected1 = 0.039;  // Округлено до 3 знаков
const diff1 = Math.abs(test1.weight - expected1);
console.log(`Ожидается: ~0.039 т (округлено с 0.03925)`);
console.log(`Получено: ${test1.weight} т`);
console.log(`📊 Статус: ${diff1 < 0.001 ? '✅ PASSED' : '❌ FAILED'}\n`);

// ============================================================================
// ТЕСТ 2: Полоса 20×100, длина 10м, ст45
// ============================================================================

console.log('📋 ТЕСТ 2: Полоса 20×100, длина 10м, ст45');
console.log('─────────────────────────────────────────────────────────────\n');

const test2 = calculateMetal({
  metalType: 'strip',
  size: '20х100',
  length: 10,
  steelType: 'ст45'
}, metalDatabase);

console.log('Параметры:');
console.log('  Размер: 20×100');
console.log('  Длина: 10 м');
console.log('  Марка стали: ст45 (7.826 т/м³)');
console.log('  Коэффициент: 2 кг/м\n');

console.log('Результат:');
console.log(`  weightPerMeter (вес 1 м): ${test2.weightPerMeter} кг`);
console.log(`  weight (вес): ${test2.weight} т\n`);

console.log('Расчёт:');
console.log('  вес = 2 × 10 × 7.826 = 156.52 кг = 0.15652 т\n');

const expected2 = 0.157;  // Округлено до 3 знаков
const diff2 = Math.abs(test2.weight - expected2);
console.log(`Ожидается: ~0.157 т (округлено с 0.15652)`);
console.log(`Получено: ${test2.weight} т`);
console.log(`📊 Статус: ${diff2 < 0.001 ? '✅ PASSED' : '❌ FAILED'}\n`);

// ============================================================================
// ТЕСТ 3: Полоса 50×500, длина 1м, ст3
// ============================================================================

console.log('📋 ТЕСТ 3: Полоса 50×500, длина 1м, ст3');
console.log('─────────────────────────────────────────────────────────────\n');

const test3 = calculateMetal({
  metalType: 'strip',
  size: '50х500',
  length: 1,
  steelType: 'ст3'
}, metalDatabase);

console.log('Параметры:');
console.log('  Размер: 50×500');
console.log('  Длина: 1 м');
console.log('  Марка стали: ст3 (7.85 т/м³)');
console.log('  Коэффициент: 25 кг/м\n');

console.log('Результат:');
console.log(`  weightPerMeter (вес 1 м): ${test3.weightPerMeter} кг`);
console.log(`  weight (вес): ${test3.weight} т\n`);

console.log('Расчёт:');
console.log('  вес = 25 × 1 × 7.85 = 196.25 кг = 0.19625 т\n');

const expected3 = 0.196;  // Округлено до 3 знаков
const diff3 = Math.abs(test3.weight - expected3);
console.log(`Ожидается: ~0.196 т (округлено с 0.19625)`);
console.log(`Получено: ${test3.weight} т`);
console.log(`📊 Статус: ${diff3 < 0.001 ? '✅ PASSED' : '❌ FAILED'}\n`);

// ============================================================================
// ТЕСТ 4: С дополнительными полями (длина 1 шт × количество)
// ============================================================================

console.log('📋 ТЕСТ 4: С дополнительными полями (длина 1 шт × количество)');
console.log('─────────────────────────────────────────────────────────────\n');

const test4 = calculateMetal({
  metalType: 'strip',
  size: '10х50',
  lengthSheet: 6,
  pieces: 5,
  steelType: 'ст3'
}, metalDatabase);

console.log('Параметры:');
console.log('  Размер: 10×50');
console.log('  Длина 1 шт: 6 м');
console.log('  Количество штук: 5 шт');
console.log('  Марка стали: ст3 (7.85 т/м³)');
console.log('  Коэффициент: 0.5 кг/м\n');

console.log('Результат:');
console.log(`  weightPerMeter (вес 1 м): ${test4.weightPerMeter} кг`);
console.log(`  actual length (общая длина): ${test4.actual.length} м`);
console.log(`  weight (вес): ${test4.weight} т\n`);

console.log('Расчёт:');
console.log('  общая_длина = 6 × 5 = 30 м');
console.log('  вес = 0.5 × 30 × 7.85 = 117.75 кг = 0.11775 т\n');

const expected4 = 0.118;  // Округлено до 3 знаков
const diff4 = Math.abs(test4.weight - expected4);
console.log(`Ожидается: ~0.118 т (округлено с 0.11775)`);
console.log(`Получено: ${test4.weight} т`);
console.log(`📊 Статус: ${diff4 < 0.001 ? '✅ PASSED' : '❌ FAILED'}\n`);

// ============================================================================
// ИТОГИ
// ============================================================================

console.log('═══════════════════════════════════════════════════════════════');
console.log('📊 ИТОГИ ТЕСТИРОВАНИЯ');
console.log('═══════════════════════════════════════════════════════════════\n');

const allPassed = (
  diff1 < 0.001 &&
  diff2 < 0.001 &&
  diff3 < 0.001 &&
  diff4 < 0.001
);

if (allPassed) {
  console.log('✅ ВСЕ ТЕСТЫ ПРОЙДЕНЫ УСПЕШНО!\n');
  console.log('🎉 Полоса работает правильно:');
  console.log('   1. Расчёт с ст3 работает (тест 1 и 3)');
  console.log('   2. Марка стали влияет на вес (тест 2)');
  console.log('   3. Дополнительные поля работают (тест 4: длина 1 шт × количество)');
  console.log('   4. Формула работает правильно: вес_кг = коэффициент × длина_м × плотность_стали\n');
  console.log('✅ Формула работает правильно!');
} else {
  console.log('❌ НЕКОТОРЫЕ ТЕСТЫ НЕ ПРОШЛИ!\n');
  console.log(`Тест 1: ${diff1 < 0.001 ? '✅' : '❌'}`);
  console.log(`Тест 2: ${diff2 < 0.001 ? '✅' : '❌'}`);
  console.log(`Тест 3: ${diff3 < 0.001 ? '✅' : '❌'}`);
  console.log(`Тест 4: ${diff4 < 0.001 ? '✅' : '❌'}`);
}

console.log('\n═══════════════════════════════════════════════════════════════');
